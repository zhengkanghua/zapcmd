# Runtime Templates Assets

该目录存放运行时模板资产（非文档）。

- `config.json`：默认配置样例
- `state.json`：状态文件样例
- `stats.json`：统计文件样例
- `combos.json`：组合文件样例
- `commands/`：命令模板与内置命令输出

文档说明请查看：`docs/runtime_templates/README.md`。
