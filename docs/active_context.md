# 短期记忆（2026-03-04）

已完成：Phase1-3（门禁、覆盖率90%、最小E2E、核心路径回归）。  
最新：Phase4/04-01：`terminal.rs` build/spawn 分离 + Rust 单测锁定 argv/拒绝/错误传播。  
最新：Phase4/04-02：终端探测 resolver 注入 + `parse_first_non_empty_line` + 探测回退/解析单测（只锁 terminal_id 集合与回退行为）。  
下一步：04-03 Rust 单测纳入门禁。  
