{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window",
  "windows": ["main", "settings"],
  "permissions": ["core:default", "core:window:allow-close", "updater:default"]
}
