---
phase: 07-robustness-errors
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/commands/runtimeLoader.ts
  - src/features/commands/schemaGuard.ts
  - src/composables/launcher/useCommandCatalog.ts
  - src/composables/settings/useCommandManagement.ts
  - src/features/settings/types.ts
  - src/i18n/messages.ts
  - src/features/commands/__tests__/runtimeLoader.test.ts
  - src/composables/__tests__/launcher/useCommandCatalog.test.ts
  - src/composables/__tests__/settings/useCommandManagement.test.ts
autonomous: true
requirements: [ROB-01]
must_haves:
  truths:
    - "用户命令目录读取失败、JSON 解析失败、schema 校验失败都能在设置页看到，不再仅写 console。"
    - "每条加载问题至少可见 sourceId + reason；可识别命令时可见 commandId。"
    - "命令加载问题有稳定错误阶段语义（read/parse/schema/merge），便于后续扩展与回归断言。"
    - "ROB-01 的失败路径有自动化回归，不会因文案或异常对象形态变化而静默退化。"
  artifacts:
    - path: "src/features/commands/runtimeLoader.ts"
      provides: "命令加载问题结构化产出（code/stage/sourceId/commandId/reason）"
      contains: "invalid-json/invalid-schema/duplicate-id/shell-ignored/read-failed 的统一 issue 生成"
    - path: "src/composables/launcher/useCommandCatalog.ts"
      provides: "读取用户命令失败可见化"
      contains: "refreshUserCommands catch 分支写入 loadIssues，而不是只 console.warn"
    - path: "src/composables/settings/useCommandManagement.ts"
      provides: "命令管理页面问题文案映射"
      contains: "格式化 issue 时输出来源、命令标识与原因片段"
    - path: "src/features/commands/__tests__/runtimeLoader.test.ts"
      provides: "加载问题契约回归"
      contains: "reason/stage 字段断言与 read/parse/schema 边界覆盖"
  key_links:
    - from: "src/composables/launcher/useCommandCatalog.ts"
      to: "src/composables/settings/useCommandManagement.ts"
      via: "loadIssues -> commandLoadIssues"
      pattern: "loadIssues\\.value|createIssueViews"
    - from: "src/composables/settings/useCommandManagement.ts"
      to: "src/i18n/messages.ts"
      via: "formatIssue 使用多语言模板渲染"
      pattern: "settings\\.commands\\.issue"
---

<objective>
为命令加载链路建立“可见、可定位、可回归”的错误语义层，完成 ROB-01 的主体闭环。

Purpose: 让用户在命令缺失或解析失败时能直接定位来源与原因，避免吞错。  
Output: 结构化加载问题模型 + 设置页展示映射 + 失败路径回归测试。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-robustness-errors/07-RESEARCH.md

@src/features/commands/runtimeLoader.ts
@src/features/commands/schemaGuard.ts
@src/composables/launcher/useCommandCatalog.ts
@src/composables/settings/useCommandManagement.ts
@src/features/settings/types.ts
@src/i18n/messages.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展命令加载问题契约（stage/reason/read-failed）</name>
  <files>src/features/commands/runtimeLoader.ts, src/features/commands/schemaGuard.ts, src/features/commands/__tests__/runtimeLoader.test.ts</files>
  <behavior>
    - 无法读取文件、JSON 解析失败、schema 不合法都能输出结构化 issue。
    - invalid-schema 至少提供首个失败原因摘要，便于定位。
    - duplicate-id / shell-ignored 继续保留 commandId，避免定位能力回退。
  </behavior>
  <action>
在 loader 层统一错误语义：
1) 为 `CommandLoadIssue` 增加 `stage` 与 `reason`；新增 `read-failed` 代码位；
2) schema 校验从纯 boolean 判定扩展到“可返回失败原因摘要”的入口（保留现有类型守卫兼容）；
3) `runtimeLoader` 在 parse/schema/merge 分支按统一字段填充 issue，并补充对应单测断言。
  </action>
  <verify>
    <automated>npm run test:run -- src/features/commands/__tests__/runtimeLoader.test.ts</automated>
  </verify>
  <done>
`runtimeLoader` 产出的 issue 具备 stage/reason 语义，且 read/parse/schema/merge 关键分支均有回归覆盖。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把读取失败接入设置页可见链路（不再吞错）</name>
  <files>src/composables/launcher/useCommandCatalog.ts, src/composables/settings/useCommandManagement.ts, src/features/settings/types.ts, src/i18n/messages.ts</files>
  <behavior>
    - `refreshUserCommands` 读盘异常时，设置页能看到失败条目。
    - issue 展示包含来源与失败原因，双语文案一致可读。
    - 现有命令过滤/分组行为不被此改动破坏。
  </behavior>
  <action>
贯通 `loadIssues -> commandLoadIssues -> SettingsCommandsSection`：
1) `useCommandCatalog` catch 分支写入 `read-failed` issue，并保留后续可重试能力；
2) `useCommandManagement` 的 issue view 承载 `stage/reason`，并更新 formatIssue 映射；
3) 在 `messages.ts` 增加/调整 ROB-01 所需文案键，确保 zh-CN/en-US 均包含来源 + 原因片段。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/launcher/useCommandCatalog.test.ts src/composables/__tests__/settings/useCommandManagement.test.ts</automated>
  </verify>
  <done>
设置页可见 read/parse/schema 失败，且展示信息满足“sourceId + reason（可选 commandId）”约束。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 追加 ROB-01 回归矩阵并固定断言口径</name>
  <files>src/features/commands/__tests__/runtimeLoader.test.ts, src/composables/__tests__/launcher/useCommandCatalog.test.ts, src/composables/__tests__/settings/useCommandManagement.test.ts</files>
  <behavior>
    - read/parse/schema 失败均有测试。
    - 断言聚焦“关键片段 + 关键字段”，避免整句文案硬编码。
    - 不引入与 ROB-01 无关的行为扩张。
  </behavior>
  <action>
补齐并收敛测试矩阵：
1) `runtimeLoader` 覆盖 reason/stage 与 commandId 可选性；
2) `useCommandCatalog` 覆盖读盘异常可见化；
3) `useCommandManagement` 覆盖新增 issue code/stage 的格式化输出。
  </action>
  <verify>
    <automated>npm run test:run -- src/features/commands/__tests__/runtimeLoader.test.ts src/composables/__tests__/launcher/useCommandCatalog.test.ts src/composables/__tests__/settings/useCommandManagement.test.ts</automated>
  </verify>
  <done>
ROB-01 失败链路具备稳定自动化回归，后续重构不会回到“日志有错、用户无感知”状态。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 命令加载失败类型（read/parse/schema）在设置页可见
- [ ] 每条失败至少可见 sourceId 与 reason（可识别时含 commandId）
- [ ] 相关单测全部通过且断言不依赖整句文案
</verification>

<success_criteria>
ROB-01 达成：命令加载/解析失败不吞错，提示可定位、可解释，并有稳定回归断言兜底。
</success_criteria>

<output>
After completion, create `.planning/phases/07-robustness-errors/07-01-SUMMARY.md`
</output>
