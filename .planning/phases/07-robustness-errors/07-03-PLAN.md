---
phase: 07-robustness-errors
plan: "03"
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/features/update/types.ts
  - src/composables/update/useUpdateManager.ts
  - src/services/updateService.ts
  - src/components/settings/parts/SettingsAboutSection.vue
  - src/i18n/messages.ts
  - src/composables/__tests__/update/useUpdateManager.test.ts
  - src/services/__tests__/updateService.test.ts
  - src/services/__tests__/startupUpdateCheck.test.ts
  - src/__tests__/app.failure-events.test.ts
autonomous: true
requirements: [ROB-03]
must_haves:
  truths:
    - "更新失败可区分 check/download/install 阶段，不再共用单一错误语义。"
    - "更新失败后可重试，按钮与状态恢复到可继续操作，不进入坏状态。"
    - "启动时自动检查更新失败保持非阻断，主流程继续可用。"
  artifacts:
    - path: "src/features/update/types.ts"
      provides: "阶段化更新错误类型契约"
      contains: "UpdateStatus.error.stage（check/download/install）"
    - path: "src/composables/update/useUpdateManager.ts"
      provides: "分阶段错误状态机 + 可重试恢复"
      contains: "check/download/install 分支写入 stage 且失败后恢复可重试"
    - path: "src/components/settings/parts/SettingsAboutSection.vue"
      provides: "About 区域按阶段渲染失败提示与下一步"
      contains: "根据 updateStatus.error.stage 选择提示与动作"
    - path: "src/composables/__tests__/update/useUpdateManager.test.ts"
      provides: "ROB-03 回归断言"
      contains: "阶段断言 + 失败后可重试 + 非阻断启动检查"
  key_links:
    - from: "src/composables/update/useUpdateManager.ts"
      to: "src/components/settings/parts/SettingsAboutSection.vue"
      via: "updateStatus.state=error 且 stage 区分渲染"
      pattern: "stage|settings\\.about\\.updateFailed"
    - from: "src/services/startupUpdateCheck.ts"
      to: "src/composables/app/useAppCompositionRoot/runtime.ts"
      via: "启动更新检查失败非阻断"
      pattern: "maybeCheckForUpdateAtStartup"
---

<objective>
完成更新链路失败阶段化与可恢复能力，闭合 ROB-03。

Purpose: 让更新失败提示准确反映发生阶段，并在失败后仍可继续使用应用和重试更新。  
Output: 更新错误阶段状态机、About 分阶段提示、可重试与非阻断回归测试。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-robustness-errors/07-RESEARCH.md
@.planning/phases/07-robustness-errors/07-01-SUMMARY.md
@.planning/phases/07-robustness-errors/07-02-SUMMARY.md

@src/features/update/types.ts
@src/composables/update/useUpdateManager.ts
@src/services/updateService.ts
@src/services/startupUpdateCheck.ts
@src/components/settings/parts/SettingsAboutSection.vue
@src/i18n/messages.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新状态机分阶段错误化（check/download/install）并保证失败后可重试</name>
  <files>src/features/update/types.ts, src/composables/update/useUpdateManager.ts, src/services/updateService.ts, src/composables/__tests__/update/useUpdateManager.test.ts, src/services/__tests__/updateService.test.ts, src/services/__tests__/startupUpdateCheck.test.ts</files>
  <behavior>
    - 更新错误状态包含失败阶段，不再只有统一 error reason。
    - check/download/install 任一失败后，状态可恢复并可重试。
    - 启动自动检查失败保持非阻断，不污染主流程状态。
  </behavior>
  <action>
改造更新失败语义与恢复路径：
1) 扩展 `UpdateStatus` 的 error 结构，新增 `stage`（check/download/install）；
2) `useUpdateManager` 在 check 与 download/install 路径分别写入对应 stage；
3) 必要时在 `updateService` 提供阶段辅助信息，保证 manager 准确归类；
4) 扩展回归测试：阶段断言、失败后重试、startup check 失败非阻断。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/update/useUpdateManager.test.ts src/services/__tests__/updateService.test.ts src/services/__tests__/startupUpdateCheck.test.ts</automated>
  </verify>
  <done>
更新链路具备“阶段清晰 + 失败可恢复 + 启动非阻断”的行为基线，不会把应用带入坏状态。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: About 区域展示阶段化失败提示与下一步动作</name>
  <files>src/components/settings/parts/SettingsAboutSection.vue, src/i18n/messages.ts, src/__tests__/app.failure-events.test.ts, src/composables/__tests__/update/useUpdateManager.test.ts</files>
  <behavior>
    - 更新失败提示按阶段差异化（check/download/install）。
    - 提示包含可执行下一步（重试检查、重试下载、稍后再试）。
    - 不影响 upToDate/available/downloading/installing 正常渲染路径。
  </behavior>
  <action>
调整 About 视图层错误呈现：
1) 根据 `updateStatus.error.stage` 渲染对应标题/正文与下一步提示；
2) 扩展 `messages.ts` 的阶段化文案键，避免复用“仅检查失败”语义；
3) 复用既有按钮事件，确保错误后可直接触发重试；
4) 在 app failure-events 与 update manager 测试补充关键可见性断言。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.failure-events.test.ts src/composables/__tests__/update/useUpdateManager.test.ts</automated>
  </verify>
  <done>
设置页可准确解释更新失败阶段与下一步，用户无需查看控制台即可执行恢复动作。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 更新失败可区分 check/download/install 阶段
- [ ] 更新失败后可重试，状态与按钮恢复可操作
- [ ] 启动自动检查失败保持非阻断
- [ ] About 提示包含阶段语义与可执行 next-step
</verification>

<success_criteria>
ROB-03 达成：更新失败链路具备“可见 + 可定位 + 可恢复”的稳定回归基线。
</success_criteria>

<output>
After completion, create `.planning/phases/07-robustness-errors/07-03-SUMMARY.md`
</output>
