---
phase: 07-robustness-errors
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/composables/execution/useCommandExecution/helpers.ts
  - src/composables/execution/useCommandExecution/actions.ts
  - src/i18n/messages.ts
  - src/composables/__tests__/execution/useCommandExecution.test.ts
  - src/__tests__/app.failure-events.test.ts
autonomous: true
requirements: [ROB-02]
must_haves:
  truths:
    - "参数必填缺失不再 silent return；用户会收到明确且可操作的下一步提示。"
    - "命令执行失败至少区分终端不可用/参数问题/安全拦截/未知错误，并映射到稳定 next-step。"
    - "单条执行与队列执行对失败分类与反馈口径保持一致。"
  artifacts:
    - path: "src/composables/execution/useCommandExecution/actions.ts"
      provides: "参数提交拒绝显式反馈 + 失败提示映射入口"
      contains: "submitParamInput 拒绝分支写入 error feedback"
    - path: "src/composables/execution/useCommandExecution/helpers.ts"
      provides: "执行失败分类与 next-step 映射"
      contains: "execution failed 分支统一分类，不直接透传 raw error"
    - path: "src/composables/__tests__/execution/useCommandExecution.test.ts"
      provides: "ROB-02 回归断言"
      contains: "required 缺失/terminal unavailable/security blocked/unknown 断言"
  key_links:
    - from: "src/composables/execution/useCommandExecution/helpers.ts"
      to: "src/composables/execution/useCommandExecution/actions.ts"
      via: "失败分类器 + 提示映射"
      pattern: "shouldRejectPendingSubmit|execution\\.failed|execution\\.blocked"
    - from: "src/composables/execution/useCommandExecution/actions.ts"
      to: "src/i18n/messages.ts"
      via: "错误分类映射到 i18n key"
      pattern: "execution\\.failed|execution\\.nextStep"
---

<objective>
完成命令执行链路的失败可操作化，闭合 ROB-02。

Purpose: 让执行失败不仅“可见”，还“可行动”，避免用户停留在“点了没反应”状态。  
Output: 参数提交拒绝显式反馈、执行失败分类器、稳定 next-step 文案与回归测试。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-robustness-errors/07-RESEARCH.md
@.planning/phases/07-robustness-errors/07-01-SUMMARY.md

@src/composables/execution/useCommandExecution/helpers.ts
@src/composables/execution/useCommandExecution/actions.ts
@src/i18n/messages.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 消除参数提交静默失败并建立执行失败 next-step 映射</name>
  <files>src/composables/execution/useCommandExecution/helpers.ts, src/composables/execution/useCommandExecution/actions.ts, src/i18n/messages.ts, src/composables/__tests__/execution/useCommandExecution.test.ts</files>
  <behavior>
    - 必填参数缺失时立即反馈错误与下一步，不再 silent return。
    - 执行失败按可操作类别输出（终端不可用/参数非法/安全拦截/未知）。
    - 单条执行与队列执行共用同一失败分类逻辑。
  </behavior>
  <action>
在执行链路引入“失败分类 -> 提示/下一步”：
1) 调整 pending 提交校验接口，返回可解释拒绝原因；
2) `submitParamInput` 命中必填缺失时设置 error feedback（含 next-step）；
3) 在执行失败捕获处引入分类函数，按类别映射 i18n key（不再仅透传 raw error）；
4) 更新 execution 单测覆盖 silent -> visible 回归。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/execution/useCommandExecution.test.ts</automated>
  </verify>
  <done>
参数必填缺失场景具备可操作反馈，执行失败类别具备稳定映射，不再出现“点击后无响应”。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 app 级失败事件断言并固定 ROB-02 回归口径</name>
  <files>src/__tests__/app.failure-events.test.ts, src/composables/__tests__/execution/useCommandExecution.test.ts</files>
  <behavior>
    - app 级失败事件可断言 next-step 语义，不依赖整句文案。
    - 队列失败与单条失败回归口径一致。
    - 不引入 ROB-03 更新链路变更。
  </behavior>
  <action>
补齐 ROB-02 回归矩阵：
1) 在 app failure-events 测试中增加执行失败分类与 next-step 断言；
2) 对 execution 单测使用“关键字段 + 关键片段”断言，避免整句硬编码；
3) 明确排除更新阶段行为，保持本 plan 聚焦执行链路。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/execution/useCommandExecution.test.ts src/__tests__/app.failure-events.test.ts</automated>
  </verify>
  <done>
ROB-02 的关键失败路径具备稳定自动化回归，后续重构不会回退到不可操作提示。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 参数必填缺失场景有显式错误反馈与 next-step
- [ ] 执行失败分类可区分至少 3 类并有自动化断言
- [ ] 单条执行与队列执行失败反馈口径一致
</verification>

<success_criteria>
ROB-02 达成：命令执行失败具备“可见 + 可定位 + 可操作”的稳定回归基线。
</success_criteria>

<output>
After completion, create `.planning/phases/07-robustness-errors/07-02-SUMMARY.md`
</output>
