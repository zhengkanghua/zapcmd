---
phase: 08-architecture-testability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/settingsStore.ts
  - src/stores/settings/defaults.ts
  - src/stores/settings/normalization.ts
  - src/stores/settings/migration.ts
  - src/stores/settings/storageAdapter.ts
  - src/stores/__tests__/settingsStore.test.ts
  - src/composables/__tests__/settings/useSettingsWindowPersistence.test.ts
autonomous: true
requirements: [ARC-02]
must_haves:
  truths:
    - "`settingsStore` 的业务计算（normalize/migrate）可脱离 localStorage 直接单测。"
    - "持久化协议保持兼容：`zapcmd.settings` 与 legacy keys 行为不回退。"
    - "Pinia store 只保留状态与编排职责，IO 通过 storage adapter 注入。"
  artifacts:
    - path: "src/stores/settings/normalization.ts"
      provides: "设置业务规范化纯函数"
      contains: "windowOpacity/hotkeys/commandView 等字段的稳定归一化规则"
    - path: "src/stores/settings/migration.ts"
      provides: "设置迁移纯函数"
      contains: "schema 版本升级与 legacy key 合并策略"
    - path: "src/stores/settings/storageAdapter.ts"
      provides: "设置存储适配层"
      contains: "read/write 接口、key 协议封装与错误边界"
    - path: "src/stores/settingsStore.ts"
      provides: "瘦身后的 Pinia 外壳"
      contains: "hydrate/persist action 编排，不内嵌复杂业务计算"
  key_links:
    - from: "src/stores/settings/migration.ts"
      to: "src/stores/settingsStore.ts"
      via: "hydrate 流程调用迁移函数输出稳定状态"
      pattern: "migrate.*Settings"
    - from: "src/stores/settings/storageAdapter.ts"
      to: "src/stores/settingsStore.ts"
      via: "persist/hydrate 统一从 adapter 读写"
      pattern: "create.*StorageAdapter|readSettings|writeSettings"
---

<objective>
完成 ARC-02：拆分 `settingsStore` 的职责边界，使业务计算可单测、IO 可替换、接口更窄更稳定。

Purpose: 降低设置链路变更成本与漏改风险，防止“业务逻辑和读写协议缠绕”。  
Output: settings 纯逻辑模块 + 存储适配层 + Pinia 薄编排 + 协议兼容回归测试。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-architecture-testability/08-RESEARCH.md

@src/stores/settingsStore.ts
@src/stores/__tests__/settingsStore.test.ts
@src/composables/__tests__/settings/useSettingsWindowPersistence.test.ts
@src/composables/settings/useSettingsWindow/persistence.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 抽离 settings 纯业务模块并建立契约测试</name>
  <files>src/stores/settings/defaults.ts, src/stores/settings/normalization.ts, src/stores/settings/migration.ts, src/stores/__tests__/settingsStore.test.ts</files>
  <behavior>
    - normalize/migrate 可在无 window/localStorage 环境下直接测试。
    - schema 与 legacy key 的兼容行为保持现状，不改语义。
    - 默认值来源集中到 defaults 模块，禁止散落魔术值。
  </behavior>
  <action>
先拆“纯函数”，再改 store：
1) 把默认值、规范化、迁移逻辑分别抽到独立模块；
2) 使用现有测试夹具迁移到新入口，先锁回归再替换实现；
3) 对高风险字段（热键、透明度、命令视图）补齐边界用例。
  </action>
  <verify>
    <automated>npm run test:run -- src/stores/__tests__/settingsStore.test.ts</automated>
  </verify>
  <done>
settings 业务计算逻辑已与 IO 解耦，且迁移/规范化关键分支具备稳定自动化断言。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 引入 storage adapter 并将 store 收敛为薄编排层</name>
  <files>src/stores/settings/storageAdapter.ts, src/stores/settingsStore.ts, src/stores/__tests__/settingsStore.test.ts</files>
  <behavior>
    - `settingsStore` 不再直接读写 localStorage，改由 adapter 抽象。
    - persist/hydrate 的外部可见行为保持一致（包括错误处理与回退）。
    - adapter 可在测试中替换为内存实现，降低环境耦合。
  </behavior>
  <action>
采用渐进替换：
1) 新增 adapter 接口与默认实现，封装 key/version 读写协议；
2) `settingsStore` action 改为调用 adapter + 纯函数模块；
3) 保留原对外 API，不做破坏式导出变更。
  </action>
  <verify>
    <automated>npm run test:run -- src/stores/__tests__/settingsStore.test.ts src/composables/__tests__/settings/useSettingsWindowPersistence.test.ts</automated>
  </verify>
  <done>
store 已完成“业务纯函数 + IO adapter + action 编排”分层，协议兼容性可回归验证。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 固化设置协议兼容回归（read→write→read 回环）</name>
  <files>src/stores/__tests__/settingsStore.test.ts, src/composables/__tests__/settings/useSettingsWindowPersistence.test.ts</files>
  <behavior>
    - 旧数据迁移后写回再读取，结果稳定且无字段漂移。
    - 持久化失败场景仍可给出可定位错误，不出现静默吞错。
    - 断言聚焦协议字段而非内部实现细节，便于后续重构。
  </behavior>
  <action>
补齐契约回归矩阵：
1) 增加 read→write→read round-trip 用例；
2) 增加 legacy key + 当前 key 共存场景；
3) 在 persistence 测试中验证 adapter 错误边界与恢复策略。
  </action>
  <verify>
    <automated>npm run test:run -- src/stores/__tests__/settingsStore.test.ts src/composables/__tests__/settings/useSettingsWindowPersistence.test.ts</automated>
  </verify>
  <done>
ARC-02 的“业务可单测 + 协议兼容 + IO 解耦”目标可由自动化测试直接证明。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `settingsStore` 职责已拆分为 defaults/normalization/migration/storage/store
- [ ] 业务纯函数无需 localStorage 依赖即可通过单测
- [ ] 协议兼容（key/schema/legacy）回归断言通过
</verification>

<success_criteria>
ARC-02 达成：至少 1 个难测 store/service 完成拆分，业务部分可单测覆盖，接口更窄且稳定。
</success_criteria>

<output>
After completion, create `.planning/phases/08-architecture-testability/08-02-SUMMARY.md`
</output>
