---
phase: 08-architecture-testability
plan: "03"
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/__tests__/app.failure-events.test.ts
  - src/stores/__tests__/settingsStore.test.ts
  - docs/architecture_plan.md
autonomous: true
requirements: [ARC-01, ARC-02]
must_haves:
  truths:
    - "Phase 8 两条重构线（组合根、settings store）在同一回归矩阵下可验证，不互相破坏。"
    - "关键门禁（单测回归 + 覆盖率/类型/构建/Rust）保持全绿，重构不引入功能回退。"
    - "新边界（ports/policies 与 settings adapter）有文档化说明，后续迭代可复用。"
  artifacts:
    - path: "src/__tests__/app.failure-events.test.ts"
      provides: "ARC-01 关键行为回归锚点"
      contains: "组合根事件桥接、失败提示与边界条件断言"
    - path: "src/stores/__tests__/settingsStore.test.ts"
      provides: "ARC-02 协议兼容回归锚点"
      contains: "迁移/规范化/持久化 round-trip 契约断言"
    - path: "docs/architecture_plan.md"
      provides: "Phase 8 架构边界文档"
      contains: "重构后模块职责、依赖方向、测试入口与禁改约束"
  key_links:
    - from: "src/composables/app/useAppCompositionRoot/ports.ts"
      to: "src/__tests__/app.failure-events.test.ts"
      via: "测试通过注入端口验证组合根行为契约"
      pattern: "create.*Ports|mock.*Ports"
    - from: "src/stores/settings/storageAdapter.ts"
      to: "src/stores/__tests__/settingsStore.test.ts"
      via: "测试验证 adapter + migration + normalization 协同回环"
      pattern: "storageAdapter|round.?trip"
---

<objective>
把 ARC-01 与 ARC-02 的成果收敛为可持续的回归与文档基线，确保 Phase 8 结束时可验证、可维护、可继续演进。

Purpose: 防止“重构完成但缺统一验收”导致后续阶段回退。  
Output: 跨模块回归矩阵 + 全量门禁校验 + 架构边界文档同步。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-architecture-testability/08-RESEARCH.md
@.planning/phases/08-architecture-testability/08-01-SUMMARY.md
@.planning/phases/08-architecture-testability/08-02-SUMMARY.md

@src/__tests__/app.failure-events.test.ts
@src/stores/__tests__/settingsStore.test.ts
@docs/architecture_plan.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 Phase 8 跨模块回归断言矩阵</name>
  <files>src/__tests__/app.failure-events.test.ts, src/stores/__tests__/settingsStore.test.ts</files>
  <behavior>
    - ARC-01 与 ARC-02 的核心行为在同一轮测试中均可定位通过/失败。
    - 断言聚焦契约行为（输入/输出/错误类别），不绑定内部实现细节。
    - 覆盖至少 1 条跨模块风险路径（例如设置变更后组合根行为仍正确）。
  </behavior>
  <action>
收敛测试入口并补齐联动风险：
1) App failure-events 增加对组合根新边界的契约断言；
2) settingsStore 测试补齐 adapter+migration+normalize 协同路径；
3) 以“行为字段 + 原因片段”替代脆弱断言，降低误报。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.failure-events.test.ts src/stores/__tests__/settingsStore.test.ts</automated>
  </verify>
  <done>
两条重构主线的关键行为可由自动化测试统一证明，失败定位可直接指向边界层。
  </done>
</task>

<task type="auto">
  <name>Task 2: 运行全量门禁并处理 Phase 8 回归缺口</name>
  <files>src/__tests__/app.failure-events.test.ts, src/stores/__tests__/settingsStore.test.ts</files>
  <action>
执行 Phase 8 最终门禁：
1) 先跑定向测试，确保新增契约断言稳定；
2) 再跑 `npm run check:all`，覆盖 lint/typecheck/test:coverage/build/check:rust；
3) 若出现回归，仅修复与 ARC-01/ARC-02 相关的问题，避免扩散改动。
  </action>
  <verify>
    <automated>npm run check:all</automated>
  </verify>
  <done>
Phase 8 重构在全量门禁下通过，不引入新的功能回归或覆盖率退化。
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新架构文档并固化后续演进约束</name>
  <files>docs/architecture_plan.md</files>
  <action>
同步文档到“可维护状态”：
1) 写明组合根 ports/policies 与 settings adapter 的职责边界；
2) 记录推荐测试入口与最小回归命令；
3) 标注禁改约束（例如不允许把纯策略重新耦合到 IO 层）。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.failure-events.test.ts src/stores/__tests__/settingsStore.test.ts</automated>
  </verify>
  <done>
文档与代码边界一致，后续 Phase 9 及以后改动可直接复用本阶段的架构与回归约束。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ARC-01 与 ARC-02 的关键行为均有稳定自动化断言
- [ ] `npm run check:all` 全绿，无新增回归
- [ ] `docs/architecture_plan.md` 已同步重构后的职责边界与测试入口
</verification>

<success_criteria>
Phase 8 结束时，重构成果具备统一回归基线与文档化边界，满足“可测试、可验证、可持续”的交付要求。
</success_criteria>

<output>
After completion, create `.planning/phases/08-architecture-testability/08-03-SUMMARY.md`
</output>
