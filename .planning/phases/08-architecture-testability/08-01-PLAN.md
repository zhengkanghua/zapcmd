---
phase: 08-architecture-testability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/app/useAppCompositionRoot/context.ts
  - src/composables/app/useAppCompositionRoot/runtime.ts
  - src/composables/app/useAppCompositionRoot/index.ts
  - src/composables/app/useAppCompositionRoot/ports.ts
  - src/composables/app/useAppCompositionRoot/policies.ts
  - src/composables/__tests__/app/useAppLifecycle.test.ts
  - src/composables/__tests__/app/useAppLifecycleBridge.test.ts
  - src/composables/__tests__/app/useAppWindowKeydown.test.ts
  - src/__tests__/app.failure-events.test.ts
autonomous: true
requirements: [ARC-01]
must_haves:
  truths:
    - "组合根的业务决策可在无 Tauri 运行时下直接单测，不再只能依赖 mount(App) 集成路径。"
    - "IO 访问（invoke/localStorage/外部窗口）通过端口注入，不在纯策略函数中直接调用。"
    - "App.vue 对 `useAppCompositionRoot` 的返回契约保持兼容，避免 UI 行为回退。"
  artifacts:
    - path: "src/composables/app/useAppCompositionRoot/ports.ts"
      provides: "组合根 IO 端口契约"
      contains: "tauri/window/storage/terminal 等外部依赖的可注入接口"
    - path: "src/composables/app/useAppCompositionRoot/policies.ts"
      provides: "组合根纯策略函数集合"
      contains: "快捷键拦截、启动触发、失败反馈映射等无副作用决策"
    - path: "src/composables/app/useAppCompositionRoot/runtime.ts"
      provides: "组合根编排层"
      contains: "通过 ports + policies 完成运行时装配，保留外部 API 兼容"
    - path: "src/composables/__tests__/app/useAppLifecycle.test.ts"
      provides: "组合根纯策略单测"
      contains: "无 Tauri 环境下的核心分支断言"
  key_links:
    - from: "src/composables/app/useAppCompositionRoot/policies.ts"
      to: "src/composables/app/useAppCompositionRoot/runtime.ts"
      via: "runtime 只消费策略结果，不直接内嵌判定逻辑"
      pattern: "create.*Policy|evaluate.*Policy"
    - from: "src/composables/app/useAppCompositionRoot/ports.ts"
      to: "src/composables/app/useAppCompositionRoot/context.ts"
      via: "context 组装默认端口，供 runtime 注入"
      pattern: "createDefault.*Ports"
---

<objective>
完成 ARC-01：重构组合根（context/runtime）使业务决策可单测，并把 IO 与决策边界明确化。

Purpose: 收窄 App 级高耦合改动面，降低未来改动触发大集成回归的频率。  
Output: 端口契约 + 纯策略函数 + 保持兼容的 runtime 装配 + 组合根单测入口。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-architecture-testability/08-RESEARCH.md

@src/App.vue
@src/composables/app/useAppCompositionRoot/context.ts
@src/composables/app/useAppCompositionRoot/runtime.ts
@src/composables/app/useAppCompositionRoot/index.ts
@src/composables/__tests__/app/useAppLifecycle.test.ts
@src/composables/__tests__/app/useAppLifecycleBridge.test.ts
@src/composables/__tests__/app/useAppWindowKeydown.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义组合根 ports/policies 契约并先补单测</name>
  <files>src/composables/app/useAppCompositionRoot/ports.ts, src/composables/app/useAppCompositionRoot/policies.ts, src/composables/__tests__/app/useAppLifecycle.test.ts, src/composables/__tests__/app/useAppWindowKeydown.test.ts</files>
  <behavior>
    - 纯策略函数可在无 Tauri、无 DOM side-effect 的环境下直接断言输入输出。
    - 所有外部副作用入口通过 ports 暴露，策略层不直接访问 invoke/localStorage。
    - 对关键失败路径（不可执行、参数缺失、窗口状态阻断）有可重复断言。
  </behavior>
  <action>
先建契约后迁移逻辑：
1) 新增 `ports.ts`，定义运行时依赖接口与默认实现工厂签名；
2) 新增 `policies.ts`，承载当前 runtime 中可抽离的纯判定函数；
3) 先写/补组合根单测，锁定政策函数期望，再迁移实现，避免行为漂移。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/app/useAppLifecycle.test.ts src/composables/__tests__/app/useAppWindowKeydown.test.ts</automated>
  </verify>
  <done>
已存在可独立运行的组合根策略单测，且 ports/policies 边界在代码层可见、可复用。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runtime/context 改为依赖注入编排并保持对外契约稳定</name>
  <files>src/composables/app/useAppCompositionRoot/context.ts, src/composables/app/useAppCompositionRoot/runtime.ts, src/composables/app/useAppCompositionRoot/index.ts, src/composables/__tests__/app/useAppLifecycleBridge.test.ts</files>
  <behavior>
    - `useAppCompositionRoot` 对 App.vue 的返回字段不删减、不重命名（除非有明确兼容层）。
    - runtime 中副作用调用仅通过 ports，决策逻辑仅通过 policies。
    - 桥接层测试能验证注入端口被按预期调用。
  </behavior>
  <action>
分两步迁移：
1) `context.ts` 负责组装默认 ports 与依赖注入参数；
2) `runtime.ts` 负责编排，调用 policies 获取决策并调用 ports 落地副作用；
3) `index.ts` 保持对外入口稳定，若新增注入参数，提供默认值避免调用方重写。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/app/useAppLifecycleBridge.test.ts src/composables/__tests__/app/useAppLifecycle.test.ts</automated>
  </verify>
  <done>
组合根实现完成“决策/副作用”解耦，App 层调用方式保持兼容，桥接路径有自动化回归。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 追加 App 级关键路径回归兜底（最小集成断言）</name>
  <files>src/__tests__/app.failure-events.test.ts, src/composables/__tests__/app/useAppLifecycle.test.ts</files>
  <behavior>
    - 关键交互链路（失败反馈、生命周期桥接）继续可回归。
    - 断言聚焦行为字段/关键片段，不依赖整句文案。
    - 不新增新的巨型 mount(App) 用例，只补最小必要兜底。
  </behavior>
  <action>
在现有测试资产上收敛断言口径：
1) 用组合根单测覆盖决策分支；
2) 保留少量 App 级回归检查事件与提示桥接；
3) 调整脆弱断言（整句文案、环境强耦合 mock）为结构化断言。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/app/useAppLifecycle.test.ts src/__tests__/app.failure-events.test.ts</automated>
  </verify>
  <done>
ARC-01 关键路径在“组合根单测 + 最小 App 集成回归”双层保障下稳定通过。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 组合根决策逻辑可在无 Tauri 环境单测通过
- [ ] runtime/context 已完成 ports/policies 解耦，且 App 外部契约未破坏
- [ ] 关键路径回归（生命周期/失败反馈）保持全绿
</verification>

<success_criteria>
ARC-01 达成：至少 1 个高耦合组合根模块完成重构，核心逻辑可单测，且不引入功能回归。
</success_criteria>

<output>
After completion, create `.planning/phases/08-architecture-testability/08-01-SUMMARY.md`
</output>
