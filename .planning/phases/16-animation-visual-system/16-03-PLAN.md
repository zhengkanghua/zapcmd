---
phase: 16-animation-visual-system
plan: "03"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/stores/settings/defaults.ts
  - src/styles.css
  - src/stores/__tests__/settingsStore.test.ts
  - src/__tests__/app.failure-events.test.ts
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "主窗口默认透明度提升到 0.96（范围仍为 0.2~1.0），整体更“实”、背景噪音更低。"
    - "相关回归测试与门禁同步通过（`npm run check:all` 全绿）。"
  artifacts:
    - path: src/stores/settings/defaults.ts
      provides: "默认透明度与范围口径"
      contains: "DEFAULT_WINDOW_OPACITY|MIN_WINDOW_OPACITY|MAX_WINDOW_OPACITY"
    - path: src/composables/app/useAppCompositionRoot/context.ts
      provides: "把 settings.windowOpacity 写入 `--ui-opacity`"
      contains: "setProperty(\"--ui-opacity\""
  key_links:
    - from: src/stores/settings/defaults.ts
      to: src/composables/app/useAppCompositionRoot/context.ts
      via: "settings 默认值驱动运行时写入 CSS 变量，需与 `src/styles.css` 默认保持一致以避免启动闪烁"
      pattern: "DEFAULT_WINDOW_OPACITY|--ui-opacity"
---

<objective>
落地 Phase 16 的第三块交付：把默认透明度从 0.92 提升到 0.96（并保持 0.2~1.0 可调范围），同步更新回归测试与门禁，确保低噪音基线在自动化验证下可回归。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-animation-visual-system/16-CONTEXT.md
@.planning/phases/16-animation-visual-system/16-RESEARCH.md

@docs/ui-redesign/04-design-system.md
@docs/ui-redesign/11-b4-visual-spec.md

@src/stores/settings/defaults.ts
@src/composables/app/useAppCompositionRoot/context.ts
@src/styles.css
@src/stores/__tests__/settingsStore.test.ts
@src/__tests__/app.failure-events.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 默认透明度提升到 0.96，并保持 0.2~1.0 范围不变（CSS 默认与 settings 默认对齐）</name>
  <files>src/stores/settings/defaults.ts, src/styles.css</files>
  <action>
1) 将 `src/stores/settings/defaults.ts` 的 `DEFAULT_WINDOW_OPACITY` 从 `0.92` 调整为 `0.96`。\n
2) 保持 `MIN_WINDOW_OPACITY=0.2`、`MAX_WINDOW_OPACITY=1.0` 不变；确保 normalization（`normalizeWindowOpacity`）回退到新的默认值。\n
3) 将 `src/styles.css` 中 `:root` 的 `--ui-opacity` 默认值同步为 `0.96`，避免应用启动初期出现“先 0.96 后回落到 0.92”的闪烁。\n
  </action>
  <verify>
运行（定向）：\n
- `npm run test:run -- src/stores/__tests__/settingsStore.test.ts`\n
手动：打开主窗口 → 设置页调整透明度滑块：仍可调 0.2~1.0，默认显示接近 96%。\n
  </verify>
  <done>
默认透明度为 0.96，范围口径不变；CSS 默认与 settings 默认一致。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新默认透明度相关回归断言，并跑通统一门禁</name>
  <files>src/stores/__tests__/settingsStore.test.ts, src/__tests__/app.failure-events.test.ts</files>
  <action>
1) 将测试中硬编码的 `0.92` 默认值断言更新为 `0.96`：\n
   - `src/stores/__tests__/settingsStore.test.ts`（migration/normalization/store 默认值与 round-trip）\n
   - `src/__tests__/app.failure-events.test.ts`（构造的 settings snapshot）\n
2) 跑通统一门禁：`npm run check:all`。\n
   - 若沙盒内 vitest/esbuild 仍触发 EPERM（历史已出现），需要在沙盒外执行并在 SUMMARY/VERIFICATION 中记录约束（禁止假通过）。\n
  </action>
  <verify>
`npm run check:all` 全绿。\n
  </verify>
  <done>
默认透明度变更在测试与门禁下可回归，VIS-02 的“更实、低噪音”基线具备自动化保障。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `DEFAULT_WINDOW_OPACITY`=0.96 且范围仍为 0.2~1.0
- [ ] `--ui-opacity` 默认值与 settings 默认一致（避免启动闪烁）
- [ ] `npm run check:all` 全绿（必要时记录环境约束，不允许假通过）
</verification>

<success_criteria>
VIS-02 的关键变化（默认透明度与低噪音基线）在代码与测试中同时落地，保证未来 UI 调整不会把默认透明度悄悄回退。
</success_criteria>

<output>
After completion, create `.planning/phases/16-animation-visual-system/16-03-SUMMARY.md`.
</output>

