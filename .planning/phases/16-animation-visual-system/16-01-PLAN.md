---
phase: 16-animation-visual-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles.css
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "主窗口交互激活态（搜索输入 focus / 结果选中 / Queue Summary Pill / 主按钮）统一使用品牌色 `#4CC9F0`，不再以绿色作为品牌主色。"
    - "主窗口成功语义只使用独立的 success 色（与品牌色区分），不会与品牌色混用。"
  artifacts:
    - path: src/styles.css
      provides: "Beta Graphite Cyan 的 brand/success 令牌与主窗口激活态落位（brand vs success 分离）"
      contains: "--ui-brand|--ui-success|queue-summary-pill|result-item--active|btn-primary"
  key_links:
    - from: src/components/launcher/parts/LauncherReviewOverlay.vue
      to: src/styles.css
      via: "Review overlay 内按钮与卡片激活态沿用全局按钮/焦点样式，必须使用 brand token"
      pattern: "btn-primary|staging-card--active|review-overlay"
---

<objective>
落地 Phase 16 的第一块交付：在不做“全仓库搬家式重构”的前提下，把主窗口的 **品牌色与成功色彻底分离**，并将所有“交互激活态”统一迁移到 Beta Graphite Cyan 的 `brand.primary=#4CC9F0`，确保绿色不再作为品牌主色。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-animation-visual-system/16-CONTEXT.md
@.planning/phases/16-animation-visual-system/16-RESEARCH.md

@docs/ui-redesign/04-design-system.md
@docs/ui-redesign/11-b4-visual-spec.md

@src/styles.css
@src/components/launcher/LauncherWindow.vue
@src/components/launcher/parts/LauncherReviewOverlay.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 引入 Beta Graphite Cyan 的 brand/success 令牌，并把主窗口交互激活态从“绿色 hardcode”迁移到 brand</name>
  <files>src/styles.css</files>
  <action>
按 Phase 16 CONTEXT 与 `docs/ui-redesign/04-design-system.md` 的 Beta Graphite Cyan 落地最小必要 token（不做大范围样式重构）：\n
1) 在 `:root` 新增/补齐 **brand/success/danger** 相关 CSS 变量：\n
   - brand：`brand.primary=#4CC9F0`，并提供可用于 rgba 的 rgb 变量（例如 `--ui-brand-rgb: 76, 201, 240`）；\n
   - success：选择“克制、低噪音、与品牌色区分”的 success（推荐直接采用 Beta 的 `#2DD4BF`，或在其附近微调）；\n
   - danger：可对齐 Beta 的 `#FB7185`（若不会扩大范围）。\n
2) 将主窗口“交互激活态”全部切到 brand（禁止使用 success）：\n
   - `.queue-summary-pill`（边框/背景/hover/focus）\n
   - `.result-item--active`、`.result-item:focus-visible`、`.result-item__hl` 与左侧强调线\n
   - `.btn-primary`（主按钮/执行按钮）与其 focus ring\n
   - 输入/按钮 focus ring（如 `.staging-card__arg input:focus-visible`）\n
   - staged feedback 高亮动画（`@keyframes staged-feedback`）\n
   目标：把当前 `rgba(55, 204, 138, …)` / `#37cc8a` 等“品牌绿色”从主窗口激活态中移除。\n
3) success 只用于成功语义（当前主要是 `.execution-feedback--success`）：\n
   - 不要把 success 用作选中/聚焦/主按钮/Queue pill。\n
4) 约束：\n
   - 不新增或重排 CSS 大结构；仅做与主窗口激活态/令牌落位直接相关的替换。\n
   - 不改 settings 的 scoped 样式（settings 视觉升级不在本 Phase）。\n
  </action>
  <verify>
运行（快速回归）：\n
- `npm run test:run -- src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts`\n
手动 UI smoke（主窗口）：\n
- 搜索框 focus、结果选中、Queue pill、主按钮均为品牌色（不再是绿色）；\n
- 执行成功反馈仍为 success 语义色，且与品牌色区分明显。\n
  </verify>
  <done>
主窗口交互激活态完成 brand 迁移；success 仅用于成功/启用语义；绿色不再作为品牌主色。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 主窗口交互激活态全部使用 brand（focus/selected/pill/primary button）
- [ ] `.execution-feedback--success` 使用独立 success 色（与 brand 清晰区分）
- [ ] 主窗口不再出现“绿色=品牌”的主要视觉信号
</verification>

<success_criteria>
VIS-01 的核心约束成立：品牌色与成功色彻底分离，绿色不再作为品牌主色；主窗口的激活态视觉语言统一且克制，为后续“低噪音 + 动效”打磨奠定基线。
</success_criteria>

<output>
After completion, create `.planning/phases/16-animation-visual-system/16-01-SUMMARY.md`.
</output>

