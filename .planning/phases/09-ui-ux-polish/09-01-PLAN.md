---
phase: 09-ui-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/launcher/useMainWindowShell.ts
  - src/features/hotkeys/windowKeydownHandlers/main.ts
  - src/features/hotkeys/windowKeydownHandlers/settings.ts
  - src/composables/launcher/useStagingQueue/focus.ts
  - src/components/settings/parts/SettingsNav.vue
  - src/styles.css
  - src/__tests__/app.hotkeys.test.ts
  - src/__tests__/app.settings-hotkeys.test.ts
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "启动器 `Esc` 保持分层后退：安全确认 → 参数弹层 → 清空查询 → 收起队列 → 隐藏主窗。"
    - "`Tab` 继续承担队列开关职责，不被改造成主界面通用遍历键。"
    - "切换到队列区时，如队列当前收起，则自动展开并把活动项/可见提示带到队列区。"
    - "高频键盘目标具备一致、可见的 focus/active 提示，不再只靠零散样式维持。"
  artifacts:
    - path: "src/composables/launcher/useMainWindowShell.ts"
      provides: "启动器分层后退契约"
      contains: "主窗 Esc 的层级顺序与最终隐藏入口"
    - path: "src/features/hotkeys/windowKeydownHandlers/main.ts"
      provides: "启动器全局/分区热键契约"
      contains: "Tab/焦点切换/结果区/队列区键盘行为"
    - path: "src/features/hotkeys/windowKeydownHandlers/settings.ts"
      provides: "设置页局部优先关闭契约"
      contains: "录制快捷键与终端下拉优先于关闭窗口"
    - path: "src/styles.css"
      provides: "高频控件 focus-visible 基础样式"
      contains: "launcher/settings 高频交互目标的统一焦点提示"
    - path: "src/__tests__/app.hotkeys.test.ts"
      provides: "启动器键盘回归"
      contains: "Esc 分层后退、队列切换、活动项/焦点行为断言"
    - path: "src/__tests__/app.settings-hotkeys.test.ts"
      provides: "设置页键盘回归"
      contains: "局部关闭优先级与不误关闭窗口断言"
  key_links:
    - from: "src/features/hotkeys/windowKeydownHandlers/main.ts"
      to: "src/composables/launcher/useMainWindowShell.ts"
      via: "启动器热键最终落到分层后退逻辑"
      pattern: "handleMainEscape|toggleStaging|switchFocusZone"
    - from: "src/composables/launcher/useStagingQueue/focus.ts"
      to: "src/__tests__/app.hotkeys.test.ts"
      via: "队列自动展开与活动项可见性的用户侧回归"
      pattern: "switchFocusZone|staging-card--active"
---

<objective>
完成 UX-01 的第一阶段：把启动器/设置页的键盘契约与焦点可见性基础固定下来，并用回归测试锁住。

Purpose: 先统一“按键怎么退、焦点怎么显、哪里会误触”这条主线，给后续 Phase 9 的视觉/反馈微调提供稳定行为底座。  
Output: 分层后退契约、队列切焦点契约、设置页局部优先关闭契约、键盘回归测试与最小焦点样式基础。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-ui-ux-polish/09-CONTEXT.md
@.planning/phases/09-ui-ux-polish/09-RESEARCH.md

@src/composables/launcher/useMainWindowShell.ts
@src/features/hotkeys/windowKeydownHandlers/main.ts
@src/features/hotkeys/windowKeydownHandlers/settings.ts
@src/composables/launcher/useStagingQueue/focus.ts
@src/components/settings/parts/SettingsNav.vue
@src/styles.css
@src/__tests__/app.hotkeys.test.ts
@src/__tests__/app.settings-hotkeys.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补启动器/设置页键盘契约回归</name>
  <files>src/__tests__/app.hotkeys.test.ts, src/__tests__/app.settings-hotkeys.test.ts</files>
  <behavior>
    - 启动器 `Esc` 的层级顺序固定为：安全确认 → 参数弹层 → 清空查询 → 收起队列 → 隐藏主窗。
    - `Tab` 继续控制队列开关，不引入新的主界面遍历语义。
    - 设置页 `Escape` 继续保持“录制快捷键 / 下拉列表先关闭，最后才关闭窗口”。
  </behavior>
  <action>
先锁测试再动实现：
1) 在 `app.hotkeys` 中把 Esc 分层后退与队列相关行为补成连续契约；
2) 在 `app.settings-hotkeys` 中补齐设置页“局部优先关闭”的稳定断言；
3) 断言聚焦行为与关键状态，不做脆弱的整句文案硬编码。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.hotkeys.test.ts src/__tests__/app.settings-hotkeys.test.ts</automated>
  </verify>
  <done>
启动器与设置页的关键键盘规则已在测试层被固定，后续实现修改有明确回归护栏。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐热键处理与焦点切换实现为最小正确行为</name>
  <files>src/composables/launcher/useMainWindowShell.ts, src/features/hotkeys/windowKeydownHandlers/main.ts, src/features/hotkeys/windowKeydownHandlers/settings.ts, src/composables/launcher/useStagingQueue/focus.ts</files>
  <behavior>
    - 只修正与 CONTEXT 决策冲突的顺序/守卫，不新增能力或新热键。
    - 队列切焦点在用户侧表现为“必要时自动展开 + 活动项正确落位”。
    - 设置页不因局部交互而误关窗口。
  </behavior>
  <action>
基于新增回归做最小实现修正：
1) 统一启动器主窗 Esc 的层级顺序；
2) 对齐焦点切换与队列展开协作逻辑，避免“想切到队列却回到搜索框”的漂移；
3) 保持设置页热键录制/下拉交互的既有局部优先级不退化。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.hotkeys.test.ts src/__tests__/app.settings-hotkeys.test.ts</automated>
  </verify>
  <done>
热键与焦点切换逻辑与锁定决策一致，且不破坏现有外部行为与入口。
  </done>
</task>

<task type="auto">
  <name>Task 3: 为高频键盘目标补齐统一 focus-visible / active 提示基础</name>
  <files>src/components/settings/parts/SettingsNav.vue, src/styles.css</files>
  <behavior>
    - launcher 与 settings 的高频交互目标在键盘操作下有一致、可见的焦点提示。
    - 不重做视觉体系，只在现有暗色/绿色强调体系中补齐基础态。
    - 不依赖鼠标 hover 才能看出当前焦点或活动区域。
  </behavior>
  <action>
围绕现有类名与语义元素做最小样式增强：
1) 为搜索输入、设置导航、选择触发器、结果项/关键按钮补齐统一 focus-visible 规则；
2) 对无法直接获得 DOM focus 的区域，沿用已有 active class 做视觉衔接；
3) 避免引入新的样式体系或复杂状态机。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.hotkeys.test.ts src/__tests__/app.settings-hotkeys.test.ts</automated>
  </verify>
  <done>
键盘用户在 launcher/settings 的高频操作中能稳定看到当前焦点或活动区域。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 启动器 `Esc` 分层后退契约已固定并有回归测试
- [ ] `Tab` 仍是队列开关，不引入额外主界面遍历语义
- [ ] 切到队列区时可自动展开并给出可见活动提示
- [ ] 设置页局部交互优先关闭，不误触发窗口关闭
</verification>

<success_criteria>
UX-01 的行为底座达成：启动器/设置页键盘可达性与焦点行为统一，且具备稳定自动化回归。
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-polish/09-01-SUMMARY.md`
</output>
