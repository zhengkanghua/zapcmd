---
phase: 09-ui-ux-polish
plan: "03"
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/__tests__/app.hotkeys.test.ts
  - src/__tests__/app.settings-hotkeys.test.ts
  - src/__tests__/app.failure-events.test.ts
  - src/components/settings/parts/__tests__/SettingsAboutSection.update-error-guidance.test.ts
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "Phase 9 的键盘契约、状态反馈与局部信息层级改动可由自动化回归直接证明。"
    - "回归断言聚焦真实用户行为与关键状态，不依赖脆弱截图或整句文案。"
    - "Phase 9 收尾必须把变更放进统一质量门禁，不留“只能手点验证”的尾巴。"
  artifacts:
    - path: "src/__tests__/app.hotkeys.test.ts"
      provides: "启动器键盘收敛回归"
      contains: "Esc/Tab/队列/活动项等 Phase 9 关键路径"
    - path: "src/__tests__/app.settings-hotkeys.test.ts"
      provides: "设置页键盘收敛回归"
      contains: "局部关闭优先级、保存反馈相关行为"
    - path: "src/__tests__/app.failure-events.test.ts"
      provides: "反馈与错误态收敛回归"
      contains: "空态/保存/loading/错误的关键可见性断言"
  key_links:
    - from: "src/__tests__/app.hotkeys.test.ts"
      to: "src/__tests__/app.failure-events.test.ts"
      via: "Phase 9 行为与反馈的跨界面收敛验证"
      pattern: "Escape|Tab|settings-ok|settings-error|terminalLoading"
---

<objective>
完成 Phase 9 收敛验收：把前两份计划的交互与反馈改动汇总到稳定回归矩阵，并通过统一质量门禁验证。

Purpose: 确保 Phase 9 不是“看起来好了”，而是“行为和反馈都能被自动化证明没有回退”。  
Output: 针对 Phase 9 的跨交互回归矩阵 + `npm run check:all` 通过证据。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-ui-ux-polish/09-CONTEXT.md
@.planning/phases/09-ui-ux-polish/09-RESEARCH.md
@.planning/phases/09-ui-ux-polish/09-01-SUMMARY.md
@.planning/phases/09-ui-ux-polish/09-02-SUMMARY.md

@src/__tests__/app.hotkeys.test.ts
@src/__tests__/app.settings-hotkeys.test.ts
@src/__tests__/app.failure-events.test.ts
@src/components/settings/parts/__tests__/SettingsAboutSection.update-error-guidance.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 Phase 9 跨界面回归矩阵并收敛断言口径</name>
  <files>src/__tests__/app.hotkeys.test.ts, src/__tests__/app.settings-hotkeys.test.ts, src/__tests__/app.failure-events.test.ts, src/components/settings/parts/__tests__/SettingsAboutSection.update-error-guidance.test.ts</files>
  <behavior>
    - 启动器与设置页的关键回归都围绕 Phase 9 锁定决策，不漂移到范围外功能。
    - 断言关注行为、类名、状态与关键片段，不引入脆弱截图或大段文案硬编码。
    - 最少覆盖 1-2 条关键键盘/焦点行为与关键反馈场景。
  </behavior>
  <action>
把前两份计划的改动收敛到统一测试矩阵：
1) 归并重复断言，避免同一行为散落多处难维护；
2) 补齐最小必要的跨界面验证（启动器键盘流 + 设置反馈/错误/加载）；
3) 清理过脆断言，保留对回归最敏感的检查点。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.hotkeys.test.ts src/__tests__/app.settings-hotkeys.test.ts src/__tests__/app.failure-events.test.ts src/components/settings/parts/__tests__/SettingsAboutSection.update-error-guidance.test.ts</automated>
  </verify>
  <done>
Phase 9 的关键用户可见行为与反馈都有稳定自动化证据，后续小改不易偷偷退化。
  </done>
</task>

<task type="auto">
  <name>Task 2: 通过统一质量门禁并只修复 Phase 9 相关回归</name>
  <files>src/__tests__/app.hotkeys.test.ts, src/__tests__/app.settings-hotkeys.test.ts, src/__tests__/app.failure-events.test.ts</files>
  <behavior>
    - `npm run check:all` 全绿。
    - 只处理由 Phase 9 改动直接引起的失败，不扩散修 unrelated 问题。
    - Phase 9 收尾结果可直接进入 execute-phase 的总结与验收。
  </behavior>
  <action>
用统一门禁收口：
1) 先跑与 Phase 9 直接相关的测试矩阵；
2) 再跑 `npm run check:all`；
3) 若失败，仅修与 Phase 9 变更直接相关的问题并回归复跑。
  </action>
  <verify>
    <automated>npm run check:all</automated>
  </verify>
  <done>
Phase 9 在统一质量门禁下可执行、可回归、可交付，不留隐藏回退点。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Phase 9 关键键盘/焦点行为具备稳定回归矩阵
- [ ] 空态/保存/loading/区域错误具备稳定回归矩阵
- [ ] `npm run check:all` 通过，且失败修复不超出 Phase 9 范围
</verification>

<success_criteria>
Phase 9 收尾达成：UX-01 / UX-02 的变更被自动化证明成立，并通过统一质量门禁。
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-polish/09-03-SUMMARY.md`
</output>
