---
phase: 12-macos-e2e-gate-alignment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-gate.yml
  - .github/workflows/release-build.yml
autonomous: true
requirements: [E2E-02]
must_haves:
  truths:
    - "`ci-gate.yml` 本身就能清楚表达：Windows desktop smoke 是 blocking gate；macOS / Linux 只有 cross-platform build/test smoke。"
    - "`release-build.yml` 本身就能清楚表达：Windows 质量门禁含 desktop smoke；macOS 不存在被误写成 blocking 的 desktop smoke release gate。"
    - "workflow 的 job/step 命名、注释与产物说明不再给后续文档造成‘macOS gate 已存在’的误导空间。"
  artifacts:
    - path: ".github/workflows/ci-gate.yml"
      provides: "CI Gate 的真实职责边界"
      contains: "Windows blocking smoke 与 macOS/Linux cross-platform smoke 的明确分层"
    - path: ".github/workflows/release-build.yml"
      provides: "Release workflow 的真实职责边界"
      contains: "Windows quality gate、bundle/publish 依赖关系与 macOS 非阻断现状"
  key_links:
    - from: ".github/workflows/ci-gate.yml"
      to: "CONTRIBUTING.md"
      via: "CI Gate responsibilities"
      pattern: "desktop smoke|cross-platform smoke"
    - from: ".github/workflows/release-build.yml"
      to: "README.md"
      via: "release gate description"
      pattern: "quality gate|bundle"
---

<objective>
澄清 CI / Release workflow 的真实职责边界，让 workflow 文件自身不再暗示 unsupported 的 macOS blocking gate。
Purpose: 关闭“workflow 看起来像支持了某能力，文档于是继续照着写”的根源。  
Output: 能被静态阅读理解的真实 workflow 语义。  
Scope note: 本计划以 job / step 命名、注释、说明收敛为主，不恢复任何 macOS blocking 行为。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-macos-e2e-gate-alignment/12-RESEARCH.md

@.github/workflows/ci-gate.yml
@.github/workflows/release-build.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收敛 CI Gate 的 job / step 命名与说明，明确 Windows-only blocking smoke</name>
  <files>.github/workflows/ci-gate.yml</files>
  <action>
在不改变当前真实行为的前提下，收敛 `ci-gate.yml` 的表达：
1) 将 Windows desktop smoke 的 job / step 名称或注释写清楚为 Windows-only blocking smoke；
2) 将 `cross-platform-smoke` 对 macOS / Linux 的职责明确写成 build/test/rust smoke，而不是 desktop smoke；
3) 若 artifact 名称或说明有歧义，一并调整为更不易误读的表述；
4) 不新增 macOS desktop smoke job，不改变现有 gating 逻辑。
  </action>
  <verify>
1) 静态阅读 `ci-gate.yml` 时，不会再误以为 macOS desktop smoke 已作为 blocking gate 存在；
2) Windows blocking smoke 与 macOS/Linux cross-platform smoke 的边界在 workflow 中一眼可见。
  </verify>
  <done>
CI Gate 的真实职责边界已固化在 workflow 文件本身，而不是依赖口头理解。
  </done>
</task>

<task type="auto">
  <name>Task 2: 收敛 Release workflow 的命名与说明，避免误导为 macOS release smoke gate</name>
  <files>.github/workflows/release-build.yml</files>
  <action>
在不改变当前真实行为的前提下，收敛 `release-build.yml`：
1) 明确 Windows `quality-gate` 中包含 desktop smoke；
2) 明确 macOS 参与的是 bundle/build，而不是已存在的 desktop smoke release gate；
3) 如有必要，补充注释或更清晰的 step 命名，避免后续 summary / docs 再误写；
4) 保持当前 release 流程逻辑稳定，不引入 unsupported 的 macOS gate。
  </action>
  <verify>
1) 静态阅读 `release-build.yml` 时，不会再误以为 macOS blocking smoke 已是 release 前置 gate；
2) Windows quality gate 与 macOS bundle 责任边界清晰可见。
  </verify>
  <done>
Release workflow 的表述与真实行为对齐，为公开文档与历史 evidence 更正提供稳定依据。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ci-gate.yml` 的表达已明确 Windows-only blocking smoke
- [ ] `release-build.yml` 的表达已明确 macOS 不存在 blocking desktop smoke gate
- [ ] workflow 没有被误改成 unsupported 的 macOS gate 语义
</verification>

<success_criteria>
只看 workflow 文件本身，后续维护者也不会再误把 macOS desktop smoke 写成已存在的 blocking gate。
</success_criteria>

<output>
After completion, create `.planning/phases/12-macos-e2e-gate-alignment/12-02-SUMMARY.md`.
</output>