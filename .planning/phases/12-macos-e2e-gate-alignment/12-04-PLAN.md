---
phase: 12-macos-e2e-gate-alignment
plan: "04"
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [E2E-02]
must_haves:
  truths:
    - "ROADMAP / REQUIREMENTS / STATE / audit 全部明确：Phase 12 关闭的是 macOS 口径漂移，而不是在 v1 偷偷补完 unsupported 的 full-matrix E2E。"
    - "`E2E-02` 的 v2 full-matrix 目标继续保留为 deferred；v1 中额外挂上的 macOS traceability 子项被诚实地收口，不再继续作为 blocker。"
    - "`STATE.md` 给出的下一步入口应是重新审计 / 完成里程碑，而不是继续追 unsupported 的 macOS blocking gate。"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap 的 Phase 10 / 12 真实描述"
      contains: "现实对齐后的 phase 目标、计划条目和结论"
    - path: ".planning/REQUIREMENTS.md"
      provides: "traceability 状态表"
      contains: "v1 macOS 子项与 v2 full-matrix 目标的准确区分"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "重新收敛后的 milestone 审计"
      contains: "blocker 已关闭、剩余项转 deferred / tech debt 的结论"
    - path: ".planning/STATE.md"
      provides: "当前工作位置与下一步入口"
      contains: "Phase 12 完成后的 next action"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "E2E-02 traceability"
      pattern: "E2E-02|deferred|Complete"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "Phase 12 completion / next action"
      pattern: "Phase 12|Complete"
---

<objective>
收敛 roadmap、requirements、state 与 milestone audit，正式把 Phase 12 的成果落到 planning 系统中。
Purpose: 让 blocker 在规划系统里真正关闭，而不是只停留在文档层。  
Output: 一个与官方能力边界一致、并能继续推进 milestone closure 的 planning 状态。  
Scope note: 本计划只修改规划与审计文档，不再触碰 runtime 或 workflow。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-macos-e2e-gate-alignment/12-RESEARCH.md
@.planning/phases/12-macos-e2e-gate-alignment/12-01-SUMMARY.md
@.planning/phases/12-macos-e2e-gate-alignment/12-02-SUMMARY.md
@.planning/phases/12-macos-e2e-gate-alignment/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 ROADMAP 与 REQUIREMENTS，正式区分 v1 口径收敛与 v2 full-matrix 目标</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
更新 planning 基础文档：
1) 调整 `.planning/ROADMAP.md` 中 Phase 10 / Phase 12 的描述与计划条目，使其不再暗示“恢复 macOS blocking gate”；
2) 在 `.planning/REQUIREMENTS.md` 中明确区分：`E2E-02` full-matrix 仍属于 v2 / deferred，而 v1 中曾额外挂起的 macOS traceability 子项现已通过 Phase 12 现实对齐收口；
3) 如果 traceability 行需要改名或备注，优先采用最少歧义的方式，不得让读者误解为“macOS blocking gate 已实现”。
  </action>
  <verify>
1) ROADMAP / REQUIREMENTS 的文字不会再把 Phase 12 理解成“恢复 unsupported 的 macOS gate”；
2) `E2E-02` 的 v1 / v2 边界在文档中是清晰的。
  </verify>
  <done>
规划系统对 Phase 12 的定位与官方现实一致：收敛漂移，而不是补完 unsupported 功能。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 STATE 与 milestone audit，关闭 blocker 并给出下一步入口</name>
  <files>.planning/STATE.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <action>
收敛当前状态与审计结论：
1) 在 `.planning/v1.0-MILESTONE-AUDIT.md` 中将当前 blocker 从“macOS gate 缺失”改为“已完成现实对齐，能力继续 deferred / tech debt”；
2) 在 `.planning/STATE.md` 中写明 Phase 12 完成后的当前 focus / blocker / next action，指向重新审计或 complete milestone；
3) 如果仍保留 tech debt，必须以 deferred / observation 的形式记录，而不是继续作为当前 blocker。
  </action>
  <verify>
1) audit 不再把 macOS local/remote/docs drift 记为当前 blocker；
2) `STATE.md` 给出的下一步命令入口清晰且与 audit 结论一致。
  </verify>
  <done>
planning 状态被真正推进到“Phase 12 已收口，可进入重新审计 / 里程碑收尾”的位置。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ROADMAP / REQUIREMENTS 已清楚区分 v1 现实收敛与 v2 deferred 目标
- [ ] audit 不再把 macOS gate 漂移记为当前 blocker
- [ ] STATE 已指向正确的下一步入口
</verification>

<success_criteria>
Phase 12 在 planning 系统中的结论被正式落地：macOS 口径漂移已关闭，full-matrix / stable macOS gate 继续作为 deferred 后续事项。
</success_criteria>

<output>
After completion, create `.planning/phases/12-macos-e2e-gate-alignment/12-04-SUMMARY.md`.
</output>