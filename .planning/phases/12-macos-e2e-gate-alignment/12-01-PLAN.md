---
phase: 12-macos-e2e-gate-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-local-gate.mjs
  - scripts/README.md
autonomous: true
requirements: [E2E-02]
must_haves:
  truths:
    - "`verify:local` 的帮助文本、dry-run 输出与运行时提示都明确表达：Windows 为 blocking desktop smoke，macOS 仅保留 experimental probe path。"
    - "Windows 自动补装链路保持不变；macOS 仍要求显式 experimental 开关，不再与旧文档发生‘默认执行’漂移。"
    - "`scripts/README.md` 与脚本真实行为完全一致，且明确保留旧 experimental 开关的兼容入口。"
  artifacts:
    - path: "scripts/verify-local-gate.mjs"
      provides: "本地 gate 的真实行为定义"
      contains: "平台策略、帮助文本、experimental 开关说明、失败提示"
    - path: "scripts/README.md"
      provides: "脚本级说明文档"
      contains: "Windows blocking / macOS experimental 的本地验证口径"
  key_links:
    - from: "scripts/README.md"
      to: "scripts/verify-local-gate.mjs"
      via: "verify:local usage / flags"
      pattern: "macos-desktop-e2e-experimental|verify:local"
---

<objective>
固化本地 `verify:local` 的真实 experimental 口径，确保脚本本身与脚本文档都明确表达“Windows blocking、macOS experimental”。
Purpose: 关闭本地入口层面对 macOS desktop smoke 的语义漂移，避免后续文档再次误写为默认 blocking gate。  
Output: 一个行为与说明完全一致的本地 gate 入口。  
Scope note: 本计划不恢复 macOS 默认 desktop smoke，也不改 GitHub workflow。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-macos-e2e-gate-alignment/12-RESEARCH.md

@scripts/verify-local-gate.mjs
@scripts/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收敛 verify-local-gate 的帮助文本、提示文案与 experimental 兼容说明</name>
  <files>scripts/verify-local-gate.mjs</files>
  <action>
在不改变当前真实语义的前提下，整理 `scripts/verify-local-gate.mjs` 中与 macOS 相关的文案：
1) 帮助文本、dry-run / skip 提示、失败提示统一表达为“macOS 仅提供 experimental probe，不是默认 blocking gate”；
2) 保留 `--macos-desktop-e2e-experimental` 与 `ZAPCMD_E2E_EXPERIMENTAL_MACOS=1` 作为兼容入口；
3) 若同一语义被多处重复，提取为共享常量，减少未来再漂移的概率；
4) 不改变 Windows 自动补装行为，也不把 macOS 改成默认执行 desktop smoke。
  </action>
  <verify>
1) `node scripts/verify-local-gate.mjs --help` 明确写出 macOS experimental 语义；
2) `node scripts/verify-local-gate.mjs --dry-run` 仍表现为默认不在 macOS 运行 blocking smoke；
3) `node scripts/verify-local-gate.mjs --macos-desktop-e2e-experimental --dry-run` 仍保留兼容入口并给出清晰提示。
  </verify>
  <done>
脚本本体不再留下任何会被误解为“macOS 默认 blocking gate”的文案歧义。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新脚本级 README，明确 Windows blocking / macOS experimental 口径</name>
  <files>scripts/README.md</files>
  <action>
更新 `scripts/README.md`：
1) 明确 `npm run verify:local` 在 Windows 会跑 blocking desktop smoke；
2) 明确 macOS 默认只跑 quality gate，experimental 开关仅用于手动探测 unsupported 路径；
3) 保留 `cargo install tauri-driver --locked`、`safaridriver --enable` 等前置说明，但不得暗示这条路径已是稳定 gate；
4) 记录旧 experimental 开关仍被保留用于兼容。
  </action>
  <verify>
1) `scripts/README.md` 中的命令和参数都能在脚本中找到对应实现；
2) 脚本文档不再出现“macOS 默认执行 desktop smoke”的表述。
  </verify>
  <done>
脚本级文档与脚本行为一一对应，为后续 README / CONTRIBUTING 对齐提供可信基础。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `verify-local-gate.mjs` 的帮助/提示文案与真实语义一致
- [ ] `scripts/README.md` 与脚本行为一致
- [ ] Windows blocking 与 macOS experimental 的边界不再混写
</verification>

<success_criteria>
本地 gate 的“脚本行为 + 脚本文档”层面完全收敛到同一现实：Windows blocking，macOS experimental / non-blocking。
</success_criteria>

<output>
After completion, create `.planning/phases/12-macos-e2e-gate-alignment/12-01-SUMMARY.md`.
</output>