---
phase: 03-core-path-regression
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/app.core-path-regression.test.ts
autonomous: true
requirements: [COV-03]
must_haves:
    truths:
      - "存在 1 条 App 集成级回归用例，覆盖：搜索 → 参数弹层填参并提交 → 入队（staging）→ 重挂载（unmount/mount）验证会话恢复 → Ctrl+Enter 执行队列 → 队列清空。"
      - "存在 1 条关键失败分支回归用例：Ctrl+Enter 执行队列时 `run()` 抛错 → 错误提示可见且包含失败原因片段 → staged queue 不丢失。"
      - "测试不做脆弱断言：不严格匹配完整命令字符串/成功文案；跨平台断言可降级（Windows 严格断言 powershell，其它平台仅断言不崩溃与关键状态）。"
    artifacts:
      - path: "src/__tests__/app.core-path-regression.test.ts"
        provides: "Phase 3 关键路径（成功 + 终端执行失败）稳定回归覆盖"
        contains: "mock createCommandExecutor().run + unmount/mount session restore + Ctrl+Enter execute"
    key_links:
      - from: "src/__tests__/app.core-path-regression.test.ts"
        to: "src/services/commandExecutor.ts"
        via: "vi.mock('../services/commandExecutor')"
        pattern: "createCommandExecutor"
      - from: "src/__tests__/app.core-path-regression.test.ts"
        to: "src/composables/launcher/useLauncherSessionState.ts"
        via: "localStorage session snapshot"
        pattern: "LAUNCHER_SESSION_STORAGE_KEY"
      - from: "src/__tests__/app.core-path-regression.test.ts"
        to: "src/composables/launcher/useTerminalExecution.ts"
        via: "run() request asserts"
        pattern: "terminalId/command"
---

<objective>
落实 COV-03：为关键用户路径（搜索 → 填参 → 入队 → 会话恢复 → 系统终端执行）补齐“可回归、可稳定”的自动化回归测试，并覆盖关键失败分支（终端执行失败/终端不可用）。

约束（来自 03-CONTEXT.md，必须严格遵守）：
- 以 App 集成测试（Vue Test Utils + jsdom + vitest）为主（不扩展桌面 E2E）
- 复用 `dispatchWindowKeydown()` 驱动热键；mock `createCommandExecutor().run()` 覆盖成功/失败
- 断言强度采取“最小稳定口径”（不匹配完整命令字符串、不依赖完整成功文案）
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-core-path-regression/03-CONTEXT.md
@.planning/phases/03-core-path-regression/03-RESEARCH.md

@src/__tests__/app.hotkeys.test.ts
@src/__tests__/app.failure-events.test.ts
@src/services/commandExecutor.ts
@src/composables/launcher/useTerminalExecution.ts
@src/composables/launcher/useLauncherSessionState.ts
@src/composables/execution/useCommandExecution/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 Phase 3 专用回归测试文件（成功链路：含会话恢复 + 终端执行）</name>
  <files>src/__tests__/app.core-path-regression.test.ts</files>
  <action>
新增 `src/__tests__/app.core-path-regression.test.ts`，风格对齐 `app.hotkeys.test.ts` / `app.failure-events.test.ts`：

1) 基础测试支架：
   - 使用 `@vue/test-utils` 挂载 `App.vue`（`attachTo: document.body`，`plugins: [createPinia()]`）
   - 提供并复用：`waitForUi()`、`mountApp()`、`dispatchWindowKeydown()`、`focusSearchAndType()`
   - `beforeEach`: `localStorage.clear()`；必要时 mock/spy `console.warn/error` 避免噪音（但不要吞掉真正失败断言）
   - `afterEach`: unmount 所有 wrapper；**避免双重 unmount**（如果测试中手动 unmount，需要同步从 wrappers 列表移除）

2) Mock（确保“系统终端执行”可被触发）：
   - mock `@tauri-apps/api/core`：`isTauri()` 返回 `false`
   - mock `@tauri-apps/api/window`：提供 `getCurrentWindow()` 返回最小可用对象（参考 `app.failure-events.test.ts`），避免 resolve window 报 warn 干扰
   - mock `@tauri-apps/plugin-updater`：`check()` 返回 `null`
   - mock `../services/commandExecutor`：`createCommandExecutor()` 返回 `{ run: runMock }`

3) 成功链路用例（必须覆盖完整链路）：
   - 通过搜索命中“带参数弹层”的内置命令（推荐：`查看容器日志`，理由见 03-RESEARCH.md）
   - 流程与最小稳定断言：
     a) 搜索 → `ArrowRight` 打开 param overlay → 填写必填参数（`#param-input-container`）→ submit（`.param-dialog`）
     b) 断言入队成功：`.staging-chip__count` 为 `"1"`；`.staging-card code` 包含输入值（例如 `my-container`）
     c) 等待 session 写入：`await waitForUi()` 后断言 `localStorage.getItem(LAUNCHER_SESSION_STORAGE_KEY)` 非空
     d) 模拟重启：`unmount()` 旧 App → 再 `mount()` 新 App → 断言会话恢复：`.staging-chip__count` 仍为 `"1"` 且 code 仍包含输入值
     e) 执行队列：`dispatchWindowKeydown('Enter', { ctrlKey: true })`
     f) 断言“系统终端执行”被触发：`runMock` 被调用；首个 request：
        - `command` 至少包含输入参数值片段（不要求完整命令匹配）
        - `terminalId`：Windows 平台可断言为 `"powershell"`；非 Windows 平台仅断言为非空字符串（避免未来平台默认终端变化导致误报）
     g) 执行后队列清空：`.staging-chip__count` 为 `"0"`
  </action>
  <verify>
运行以下命令验证回归用例可稳定通过：
1) `npm run test:run -- src/__tests__/app.core-path-regression.test.ts`
2) `npm run test:run`（全量，确保不引入全局副作用）
  </verify>
  <done>
- 成功链路回归用例覆盖了 Phase 3 锁定的全链路（含 unmount/mount 会话恢复验证）。  
- “系统终端执行”使用最小稳定口径断言：`run()` 被调用 + 关键片段匹配 + 成功后队列清空。  
  </done>
</task>

<task type="auto">
  <name>Task 2: 补齐关键失败分支回归（终端执行失败：错误可见 + 队列不丢失）</name>
  <files>src/__tests__/app.core-path-regression.test.ts</files>
  <action>
在同一测试文件新增失败分支用例，严格复用与成功链路一致的交互路径（至少覆盖：搜索 → 参数弹层填参并提交 → 入队 → Ctrl+Enter 执行队列），只改变 `runMock` 行为：

- `runMock.mockRejectedValueOnce(new Error('terminal-unavailable'))`

最小稳定断言：
1) `runMock` 被调用（确保确实走到“系统终端执行”的集成点）
2) 错误提示可见：`.execution-feedback--error` 文本包含 `'terminal-unavailable'`（只断言 reason 片段，避免 i18n/文案结构变动）
3) staged queue 不丢失：`.staging-chip__count` 仍为 `"1"`（便于用户重试/修改）

跨平台注意：
- 不引入平台条件分支导致崩溃；如果需要平台差异断言，只能做“更弱”的断言（例如 terminalId 非空），并在测试注释/describe 文案中明确原因。
  </action>
  <verify>
1) `npm run test:run -- src/__tests__/app.core-path-regression.test.ts`  
2) `npm run check:all`（合并门禁全绿，保证 CI 一致）
  </verify>
  <done>
- 失败分支用例能稳定覆盖：终端执行失败时错误可见、队列保留。  
- 不引入脆弱断言与平台依赖性崩溃。  
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 至少 1 条成功路径用例覆盖完整链路（含 session restore + Ctrl+Enter 执行 + 队列清空）
- [ ] 至少 1 条失败分支用例覆盖终端执行失败（错误可见 + 队列不丢失）
- [ ] 断言强度遵守“最小稳定口径”，避免完整命令字符串/成功文案匹配
- [ ] 非 Windows 平台断言不至于误报（允许降级为“不崩溃 + 关键状态”）
</verification>

<success_criteria>
- 满足 ROADMAP.md 的 Phase 3 Success Criteria（关键路径成功 + 关键失败分支 + 跨平台不崩溃）
- 满足 REQUIREMENTS.md 的 COV-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-path-regression/03-01-SUMMARY.md`
</output>

