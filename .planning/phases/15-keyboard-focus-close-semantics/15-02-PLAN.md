---
phase: 15-keyboard-focus-close-semantics
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/launcher/parts/LauncherReviewOverlay.vue
autonomous: true
requirements: [KEY-02, KEY-03]
must_haves:
  truths:
    - "Review 打开后，焦点进入 Review panel（`document.activeElement` 位于 `.review-panel` 内），不会停留在背景 Search。"
    - "`switchFocus` 打开 Review 时，焦点优先落在队列列表的当前项（或其等价焦点锚点），保证键盘用户可立即使用 ↑/↓/Delete 等队列 hotkeys。"
    - "Review 态下 `Tab/Shift+Tab` 仅在 Review 内循环焦点，并 `stopPropagation()` 防止 window hotkey 层把 Tab 误判为 `toggleQueue`。"
  artifacts:
    - path: src/components/launcher/parts/LauncherReviewOverlay.vue
      provides: "Review panel focus trap + 初始焦点进入策略（与 Param/Safety overlay 同构）"
      contains: "on.*Keydown|stopPropagation\\(\\)|tabindex"
  key_links:
    - from: src/components/launcher/parts/LauncherReviewOverlay.vue
      to: src/features/hotkeys/windowKeydownHandlers/main.ts
      via: "组件内 Tab trap 阻断事件冒泡，避免触发 toggleQueue(Tab) 全局热键"
      pattern: "stopPropagation\\(\\)"
---

<objective>
补齐 Review overlay 的 **焦点进入与 Tab focus trap**：
- Review 打开后焦点必须进入 Review（至少落在可交互元素上）。
- `switchFocus` 打开时焦点应落到队列列表/当前队列项（或其等价锚点）。
- Review 态下 `Tab/Shift+Tab` 仅在 Review 内循环，不把焦点送回背景 Search。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-keyboard-focus-close-semantics/15-CONTEXT.md
@.planning/phases/15-keyboard-focus-close-semantics/15-RESEARCH.md

@docs/ui-redesign/08-b4-interaction-state-machine.md
@docs/ui-redesign/09-b4-hotkey-migration-map.md

@src/components/launcher/parts/LauncherParamOverlay.vue
@src/components/launcher/parts/LauncherSafetyOverlay.vue
@src/components/launcher/parts/LauncherReviewOverlay.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review panel 增加 Tab focus trap（仅 plain Tab；循环 focusable；阻断冒泡）</name>
  <files>src/components/launcher/parts/LauncherReviewOverlay.vue</files>
  <action>
按 Param/Safety overlay 已验证模式实现 Review 的 focus trap：
1) 在 Review panel 容器上监听 `keydown`。
2) 仅处理 **plain** `Tab/Shift+Tab`（无 Ctrl/Meta/Alt），并在处理时：
   - 计算 panel 内 focusable 元素（button/input/textarea/[tabindex] 等）；
   - `event.preventDefault()` + 将焦点循环到下一个/上一个；
   - `event.stopPropagation()`，避免 window hotkey handler 把 `Tab` 当成 `toggleQueue`。
3) 不要手写新的选择器集合：复用 Param/Safety 的 focusable selector 列表即可。
  </action>
  <verify>
运行：`npm run test:run -- src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts`
并在 App UI 中人工验证：Review 打开后按 Tab 不会跳回搜索框/背景，且不会关闭 Review。
  </verify>
  <done>
Review 态下 Tab/Shift+Tab 被稳定 trap 在 Review 内部，不会冒泡触发全局 hotkey。
  </done>
</task>

<task type="auto">
  <name>Task 2: Review 打开后焦点进入 + switchFocus 落点（当前队列项锚点）</name>
  <files>src/components/launcher/parts/LauncherReviewOverlay.vue</files>
  <action>
补齐 Review 的初始焦点策略：
1) Review 打开后（`stagingExpanded=true` / `stagingDrawerState=open|opening`），焦点必须进入 `.review-panel` 内：
   - 优先落到“当前队列项”的可聚焦锚点（建议对 active card 采用 roving tabindex：active=0，其余=-1）；
   - 若队列为空，则落到 header 的关闭按钮（或等价的首个可交互元素）。
2) 约束：不得把焦点默认送到参数输入框（避免影响 ↑/↓/Delete hotkeys）；用户主动点击输入框时应保持输入可用。
  </action>
  <verify>
手动：
1) 搜索态按 `switchFocus`（默认 Ctrl+Tab）打开 Review → 焦点应落在当前队列项锚点；
2) 打开 Review 后反复按 Tab/Shift+Tab → 焦点始终在 Review 内循环；
3) 关闭 Review → 焦点恢复到 `#zapcmd-search-input`（由既有 closeStagingDrawer + searchFocus 负责）。
  </verify>
  <done>
Review 打开后的焦点进入与 switchFocus 落点成立，满足 KEY-02/KEY-03，并为后续自动化回归提供可定位断言锚点。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Review 打开后焦点进入 Review panel（不滞留背景 Search）
- [ ] switchFocus 打开时焦点落到队列列表/当前项锚点
- [ ] Review 态 plain Tab/Shift+Tab 仅在 Review 内循环，且阻断冒泡
</verification>

<success_criteria>
键盘用户在 Review 态具备稳定的 focus trap 与可预测的初始焦点落点：Tab 不再承担 toggleQueue 语义，且焦点不会泄漏到背景 Search。
</success_criteria>

<output>
After completion, create `.planning/phases/15-keyboard-focus-close-semantics/15-02-SUMMARY.md`.
</output>
