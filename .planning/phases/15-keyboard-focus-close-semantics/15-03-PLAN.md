---
phase: 15-keyboard-focus-close-semantics
plan: "03"
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - src/__tests__/app.hotkeys.test.ts
  - src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts
  - src/composables/__tests__/launcher/useMainWindowShell.test.ts
  - src/composables/__tests__/app/useAppWindowKeydown.test.ts
  - src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts
autonomous: true
requirements: [TST-01]
must_haves:
  truths:
    - "P0 自动化回归覆盖：toggleQueue（搜索态打开 Review）、switchFocus（打开并聚焦列表）、Esc（分层后退 Review 优先）、Review 内 Tab trap（不泄漏到 Search）。"
    - "floor height 的“无假结果”约束不回归（Phase 13 已有断言，Phase 15 仅确保本轮改动不破坏）。"
  artifacts:
    - path: src/__tests__/app.hotkeys.test.ts
      provides: "App UI 级 P0 热键回归：toggleQueue/switchFocus/Esc/Tab focus trap"
      contains: "Tab|Ctrl\\+Tab|Escape|review"
    - path: src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts
      provides: "Review 组件级焦点/Tab trap 断言（可定位且不依赖肉眼）"
      contains: "Tab|focus|review-panel"
  key_links:
    - from: src/components/launcher/parts/LauncherReviewOverlay.vue
      to: src/__tests__/app.hotkeys.test.ts
      via: "测试以 DOM 焦点与 keydown 事件回归锁定 Tab trap 与焦点进入"
      pattern: "document\\.activeElement|dispatch.*Keydown"
---

<objective>
补齐并更新 Phase 15 的 P0 自动化回归（TST-01）：把热键语义与焦点契约变成可定位断言，防止 B4 后续演进导致回归（尤其是 Tab / toggleQueue 冲突与 Esc 优先级）。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-keyboard-focus-close-semantics/15-CONTEXT.md
@.planning/phases/15-keyboard-focus-close-semantics/15-RESEARCH.md

@src/__tests__/app.hotkeys.test.ts
@src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts
@src/composables/__tests__/launcher/useMainWindowShell.test.ts
@src/composables/__tests__/app/useAppWindowKeydown.test.ts
@src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 App UI 热键回归（Tab 只负责打开；Review 内 Tab 为 focus trap；Esc 关闭 Review）</name>
  <files>src/__tests__/app.hotkeys.test.ts</files>
  <action>
按 KEY-01/KEY-04 与 Phase 15 决策更新断言：\n
1) `toggleQueue`（默认 Tab）在搜索态打开 Review；\n
2) Review 打开后再次按 Tab **不应**关闭 Review（Tab 归还焦点遍历）；\n
3) Review 打开时按 Esc 关闭 Review（并且即使 query 非空也应先关 Review，而不是先清空 query）。\n
同时补齐 switchFocus（Ctrl+Tab）的回归：在搜索态按 Ctrl+Tab 打开 Review 并把焦点送入 Review。
  </action>
  <verify>
运行：`npm run test:run -- src/__tests__/app.hotkeys.test.ts`
  </verify>
  <done>
App 级热键回归覆盖 Phase 15 的关键行为变更，且断言可定位（不依赖“看起来像”）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 unit 回归（windowKeydownHandlers / useMainWindowShell / useAppWindowKeydown）</name>
  <files>src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts, src/composables/__tests__/launcher/useMainWindowShell.test.ts, src/composables/__tests__/app/useAppWindowKeydown.test.ts</files>
  <action>
1) `useMainWindowShell`：更新 Esc 顺序断言（Review 优先关闭，再清空 query）。\n
2) `windowKeydownHandlers`：补一个关键护栏用例：当 Review 打开且 `toggleQueue=Tab` 时，`Tab` 不应触发 `toggleStaging()`。\n
3) `useAppWindowKeydown`：保持 Ctrl+Tab 路由到“打开 Review 并聚焦列表”的链路（至少保证 `openStagingDrawer` + focus-zone 的切换入口仍被调用/可定位）。\n
  </action>
  <verify>
运行：\n
- `npm run test:run -- src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts`\n
- `npm run test:run -- src/composables/__tests__/launcher/useMainWindowShell.test.ts`\n
- `npm run test:run -- src/composables/__tests__/app/useAppWindowKeydown.test.ts`
  </verify>
  <done>
关键语义变更在 unit 层具备护栏断言：Tab 不回归为 toggleQueue（Review 态），Esc 顺序不回归。
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 Review 组件级回归：focus 进入 + Tab trap 不泄漏</name>
  <files>src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts</files>
  <action>
新增/调整组件级回归断言：\n
1) Review 打开后（mounted）能把焦点送入 `.review-panel` 内（至少首个可交互元素/active item）。\n
2) 在 Review 内派发 plain `Tab/Shift+Tab`：焦点仍留在 Review 内，且事件被阻断冒泡（可通过断言 window handler 未被触发/或 activeElement 始终在 panel 内间接证明）。\n
3) 不引入 brittle 的“具体第几个按钮”断言；优先断言 `panel.contains(document.activeElement)`，避免 UI 微调导致测试震荡。
  </action>
  <verify>
运行：`npm run test:run -- src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts`
  </verify>
  <done>
Review 的焦点进入与 focus trap 具备组件级护栏，能在不依赖肉眼的情况下定位回归。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check:all` 全绿
- [ ] App/UI 回归覆盖 toggleQueue/switchFocus/Esc/Tab trap
- [ ] Unit 回归覆盖 Esc 顺序与 Tab 不误触发 toggleQueue
</verification>

<success_criteria>
Phase 15 的热键与焦点契约被自动化回归锁死：后续对 B4 的结构/动画/视觉迭代不会把 Tab 再次回归为“关 Review”的误语义，也不会把 Esc 优先级顺序改坏。
</success_criteria>

<output>
After completion, create `.planning/phases/15-keyboard-focus-close-semantics/15-03-SUMMARY.md`.
</output>

