---
phase: 06-security-regression
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/app.failure-events.test.ts
  - src/__tests__/app.hotkeys.test.ts
  - src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts
  - src/features/hotkeys/windowKeydownHandlers/index.ts
autonomous: true
requirements: [SEC-01]
must_haves:
    truths:
      - "危险命令确认弹层在确认前不会被执行热键绕过，尤其 Ctrl+Enter 不得直通执行。"
      - "取消确认（Esc/遮罩点击）后保留队列与输入状态，不发生隐式清空。"
      - "安全拦截失败反馈在用户可见层稳定存在，并且执行函数不被调用。"
      - "关键拦截提示在 zh-CN 与 en-US 均有回归断言，避免单语种退化。"
    artifacts:
      - path: "src/__tests__/app.failure-events.test.ts"
        provides: "App 级安全失败与确认流程回归"
        contains: "dangerous confirm/cancel/bypass + blocked feedback + non-execution"
      - path: "src/__tests__/app.hotkeys.test.ts"
        provides: "热键交互层回归"
        contains: "Esc/overlay 取消与安全弹层状态保持"
      - path: "src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts"
        provides: "window keydown 纯处理器防绕过断言"
        contains: "safetyDialogOpen 下 Enter/Escape/组合键行为"
    key_links:
      - from: "src/features/hotkeys/windowKeydownHandlers/index.ts"
        to: "src/composables/execution/useCommandExecution/state.ts"
        via: "confirmSafetyExecution/cancelSafetyExecution"
        pattern: "safetyDialogOpen"
      - from: "src/__tests__/app.failure-events.test.ts"
        to: "src/features/security/commandSafety.ts"
        via: "参数注入触发 blocked + UI feedback"
        pattern: "execution-feedback--error"
---

<objective>
在 App 用户可见层补齐 SEC-01 的确认/取消/绕过与失败提示回归，锁定“不可绕过 + 可见错误 + 不执行”行为。

Purpose: 让安全基线不仅在逻辑层成立，也在真实键盘交互与弹层流程上稳定可回归。  
Output: App 级与热键处理器级的安全回归用例完善，并在必要时做最小行为修正。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-security-regression/06-CONTEXT.md
@.planning/phases/06-security-regression/06-RESEARCH.md
@.planning/phases/06-security-regression/06-01-SUMMARY.md

@src/features/hotkeys/windowKeydownHandlers/index.ts
@src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts
@src/__tests__/app.hotkeys.test.ts
@src/__tests__/app.failure-events.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 增加热键处理器“防绕过”回归（safety dialog 打开态）</name>
  <files>src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts, src/features/hotkeys/windowKeydownHandlers/index.ts</files>
  <behavior>
    - safetyDialog 打开时，普通 Enter 仅作为确认动作。
    - safetyDialog 打开时，Ctrl+Enter 不触发队列执行直通（不可绕过）。
    - Escape 继续走取消确认动作。
  </behavior>
  <action>
在处理器级新增防绕过测试并在必要时做最小实现修正：
1) 新增 Ctrl+Enter 场景断言：不得触发 executeStaged，不得绕过确认门禁；
2) 保留 Enter/Escape 既有确认/取消语义；
3) 若当前实现把 Ctrl+Enter 也当确认，按 CONTEXT 决策最小化修正判断条件。
  </action>
  <verify>
    <automated>npm run test:run -- src/features/hotkeys/__tests__/windowKeydownHandlers.test.ts</automated>
  </verify>
  <done>
热键处理器层对 safety dialog 的绕过路径有明确回归保护，且核心交互未回归。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 App 级确认取消与状态保持回归</name>
  <files>src/__tests__/app.hotkeys.test.ts, src/__tests__/app.failure-events.test.ts</files>
  <behavior>
    - 危险命令触发 safety dialog 后，未确认前不执行。
    - 取消确认（Esc/遮罩）后，staged queue/输入状态保持原状。
    - safety dialog 打开时重复执行热键不产生绕过执行。
  </behavior>
  <action>
在 App 集成测试补齐真实交互链路：
1) 复用现有挂载与键盘派发工具，增加“打开 safety dialog -> Ctrl+Enter”断言；
2) 增加取消后状态保持断言（队列数量、参数输入值或 staged preview）；
3) 保持现有 confirm 路径断言，避免“防绕过”改动误伤正常确认。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.hotkeys.test.ts src/__tests__/app.failure-events.test.ts</automated>
  </verify>
  <done>
App 层确认/取消/绕过三类关键路径均可回归，行为与 CONTEXT 锁定决策一致。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 增加双语 blocked 提示关键断言（zh-CN + en-US）</name>
  <files>src/__tests__/app.failure-events.test.ts</files>
  <behavior>
    - zh-CN 场景断言包含“执行已拦截”前缀和原因片段。
    - en-US 场景断言包含 “Execution blocked” 前缀和原因片段。
    - 两种语言场景都断言 runMock 未被调用。
  </behavior>
  <action>
在现有失败事件测试中补齐双语关键断言：
1) 构造语言切换场景（通过 settings store 或启动快照）；
2) 采用“前缀 + 原因片段”断言，不硬编码整句；
3) 明确非执行断言，确保不吞错口径成立。
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/app.failure-events.test.ts</automated>
  </verify>
  <done>
安全失败提示在中英文均有稳定回归，且拦截场景明确“不执行 + 可见错误”。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] safety dialog 打开态下 Ctrl+Enter 不能绕过执行门禁
- [ ] 取消确认后队列/输入状态保持
- [ ] zh-CN 与 en-US 的 blocked 关键提示均有断言
</verification>

<success_criteria>
App 用户可见安全回归链路完整覆盖“确认/取消/绕过 + 提示可见 + 不执行”，满足 SEC-01 且与 Phase 06 上下文决策一致。
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-regression/06-02-SUMMARY.md`
</output>
