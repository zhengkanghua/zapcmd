---
phase: 06-security-regression
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/security/__tests__/commandSafety.test.ts
  - src/composables/__tests__/execution/useCommandExecution.test.ts
  - src/features/security/commandSafety.ts
  - src/composables/execution/useCommandExecution/actions.ts
autonomous: true
requirements: [SEC-01]
must_haves:
    truths:
      - "参数注入规则在 number/text 参数上都具备稳定回归：允许值可执行、高风险符号被拦截。"
      - "边界输入（前后空白）按 trim 后规则处理，不因格式噪音误判。"
      - "队列任一项命中注入拦截时整队阻断，且不会触发终端执行调用。"
      - "blocked 反馈包含稳定前缀与原因片段，避免静默吞错。"
    artifacts:
      - path: "src/features/security/__tests__/commandSafety.test.ts"
        provides: "安全判定纯逻辑回归矩阵（allow/block/boundary/queue-fail-fast）"
        contains: "INJECTION_PATTERN 符号集覆盖、trim 边界、队列阻断断言"
      - path: "src/composables/__tests__/execution/useCommandExecution.test.ts"
        provides: "执行编排层回归（blocked 不执行 + 危险确认前后行为）"
        contains: "runCommandInTerminal/runCommandsInTerminal 调用门禁断言"
    key_links:
      - from: "src/composables/execution/useCommandExecution/actions.ts"
        to: "src/features/security/commandSafety.ts"
        via: "checkSingleCommandSafety/checkQueueCommandSafety"
        pattern: "blockedMessage|confirmationItems|confirmationReasons"
---

<objective>
补齐 SEC-01 在逻辑层与执行编排层的稳定回归矩阵，优先锁定“允许/拦截/边界 + 队列整队阻断”。

Purpose: 让安全策略回归先在低耦合测试层固定，减少仅依赖 App 大集成测试的脆弱性。  
Output: `commandSafety` 与 `useCommandExecution` 的可复现安全回归用例集。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-security-regression/06-CONTEXT.md
@.planning/phases/06-security-regression/06-RESEARCH.md

@src/features/security/commandSafety.ts
@src/composables/execution/useCommandExecution/actions.ts
@src/features/security/__tests__/commandSafety.test.ts
@src/composables/__tests__/execution/useCommandExecution.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 commandSafety 纯逻辑回归矩阵（allow/block/boundary）</name>
  <files>src/features/security/__tests__/commandSafety.test.ts</files>
  <behavior>
    - number/text 各至少 1 条允许样例不触发 blocked。
    - 高风险符号集（`;`, `|`, `&`, `` ` ``, `<`, `>`, 换行, `$()`, `${}`）命中时返回 blockedMessage。
    - 前后空白输入按 trim 后规则处理（允许值可通过、非法值仍拦截）。
    - 队列任一项失败时返回 queue blocked 前缀并清空 confirmationItems。
  </behavior>
  <action>
补齐并收敛 `commandSafety.test.ts`：
1) 用参数化样例统一覆盖高风险符号集，避免重复/漏测；
2) 增加 number + text 允许路径样例，确保“拦截增强”不会误伤正常输入；
3) 增加 trim 边界样例，固定边界口径；
4) 保持断言粒度为“关键片段”，不做整句文案硬编码。
  </action>
  <verify>
    <automated>npm run test:run -- src/features/security/__tests__/commandSafety.test.ts</automated>
  </verify>
  <done>
`commandSafety.test.ts` 明确覆盖 allow/block/boundary/queue-fail-fast，且测试通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 useCommandExecution 安全门禁编排回归</name>
  <files>src/composables/__tests__/execution/useCommandExecution.test.ts</files>
  <behavior>
    - blocked 时 runCommandInTerminal/runCommandsInTerminal 均不被调用。
    - 队列安全检查命中拦截后，保留队列并给出 error 反馈。
    - 危险确认路径仍保持“未确认不执行、确认后执行”。
  </behavior>
  <action>
在 composable 测试层补齐“安全判定 -> 执行门禁”联动：
1) 增加队列命中注入拦截时的执行调用断言；
2) 增加边界输入样例在执行层的反馈断言；
3) 保持既有危险确认成功路径断言，防止回归被误改。
  </action>
  <verify>
    <automated>npm run test:run -- src/composables/__tests__/execution/useCommandExecution.test.ts</automated>
  </verify>
  <done>
执行编排层在安全命中场景下稳定阻断执行，回归用例通过且行为与 CONTEXT 决策一致。
  </done>
</task>

<task type="auto">
  <name>Task 3: 仅在测试揭示偏差时做最小修正并回归</name>
  <files>src/features/security/commandSafety.ts, src/composables/execution/useCommandExecution/actions.ts</files>
  <action>
若新增回归用例暴露真实行为偏差，仅做最小修正：
1) 保持现有 API/调用链不扩张；
2) 修正点限定在安全判定或 blocked 反馈拼接；
3) 不引入新策略或范围外能力（例如下一步文案增强）。
  </action>
  <verify>
    <automated>npm run test:run -- src/features/security/__tests__/commandSafety.test.ts src/composables/__tests__/execution/useCommandExecution.test.ts</automated>
  </verify>
  <done>
新增回归全部通过，且未引入超出 Phase 06 边界的功能变更。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 允许/拦截/边界三类样例均在逻辑层固定
- [ ] 队列命中拦截时不会触发终端执行
- [ ] 断言粒度遵循“前缀 + 原因片段”，不做整句硬编码
</verification>

<success_criteria>
`commandSafety` 与 `useCommandExecution` 层形成稳定安全回归基线，满足 SEC-01 的 allow/block/boundary 主体要求且不越 Phase 边界。
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-regression/06-01-SUMMARY.md`
</output>
