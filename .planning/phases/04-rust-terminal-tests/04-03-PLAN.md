---
phase: 04-rust-terminal-tests
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - package.json
  - scripts/precommit-guard.mjs
  - .github/workflows/ci-gate.yml
autonomous: true
requirements: [RUST-01]
must_haves:
    truths:
      - "Rust 单元测试会在 CI Gate 的 Windows/macOS/Ubuntu 都被执行（避免仅 Windows 跑 Rust 导致 `cfg(target_os)` 测试缺失）。"
      - "`npm run check:all` 包含 `cargo test --manifest-path src-tauri/Cargo.toml`，作为合并门禁的一部分。"
      - "本地 pre-commit 对高风险 Rust 变更会运行 Rust 单测（至少覆盖 `src-tauri/src/terminal.rs` 等高风险文件），避免改动只通过 cargo check。"
    artifacts:
      - path: "package.json"
        provides: "Rust 单测脚本与合并门禁整合"
        contains: "test:rust + check:all 串联"
      - path: ".github/workflows/ci-gate.yml"
        provides: "CI Gate 三平台 Rust 单测执行"
        contains: "dtolnay/rust-toolchain + Linux deps + cargo test"
      - path: "scripts/precommit-guard.mjs"
        provides: "本地高风险 Rust 变更触发 Rust 单测"
        contains: "terminal.rs/command_catalog.rs/bounds.rs 命中时追加 cargo test"
    key_links:
      - from: ".github/workflows/ci-gate.yml"
        to: "package.json"
        via: "npm run check:all / npm run test:rust"
        pattern: "check:all"
---

<objective>
把 Phase 4 的 Rust 单测真正纳入“可回归”链路：本地门禁与 CI 门禁都必须执行 `cargo test`，并覆盖三平台（Windows/macOS/Linux），避免 `cfg(target_os)` 测试永远不运行。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rust-terminal-tests/04-RESEARCH.md

@package.json
@scripts/precommit-guard.mjs
@.github/workflows/ci-gate.yml
@.github/workflows/release-build.yml
@src-tauri/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加 `test:rust` 脚本并纳入 `check:all`</name>
  <files>package.json</files>
  <action>
1) 在 `scripts` 增加：
   - `test:rust`: `cargo test --manifest-path src-tauri/Cargo.toml`

2) 更新 `check:all`：
   - 在现有 `npm run check:rust` 之后追加 `npm run test:rust`
   - 保持现有顺序与门禁不被弱化（只允许增强，不允许跳过）

备注：
- `check:rust` 仍保留（用于快速检查与 precommit 的轻量路径）
- `test:rust` 作为更强门禁用于 CI/check:all
  </action>
  <verify>
1) `npm run test:rust`  
2) `npm run check:all`（本地可跑则全跑；若耗时过长至少确保命令串联无误）
  </verify>
  <done>
- `npm run test:rust` 可独立运行，并被 `npm run check:all` 覆盖。  
  </done>
</task>

<task type="auto">
  <name>Task 2: precommit-guard 在高风险 Rust 变更时追加运行 Rust 单测</name>
  <files>scripts/precommit-guard.mjs</files>
  <action>
目标：遵守“最小阻塞”的同时确保高风险 Rust 变更必回归。

1) 保持现有行为：任何 Rust 变更仍执行 `cargo check --manifest-path src-tauri/Cargo.toml`

2) 新增高风险触发规则（与脚本内现有 highRiskRustTargets 保持一致口径）：
   - 当 staged 文件命中以下任一项时，追加运行：
     - `cargo test --manifest-path src-tauri/Cargo.toml`
   - 高风险 Rust 目标建议复用现有列表：
     - `src-tauri/src/terminal.rs`
     - `src-tauri/src/command_catalog.rs`
     - `src-tauri/src/bounds.rs`

3) 输出要求：
   - 命中高风险 Rust 单测追加时，在控制台输出一行可读提示（说明哪些文件触发、将运行 cargo test）

注意：
- 不新增“跳过 Rust 单测”的后门开关（按项目准则：不允许静默降级/回退）
  </action>
  <verify>
在本地创建一个临时分支/临时变更：仅 stage `src-tauri/src/terminal.rs` 的无害改动，运行 `npm run precommit:guard`，应看到追加的 `cargo test ...` 被执行。
  </verify>
  <done>
- 高风险 Rust 变更会被 Rust 单测阻断回归。  
- 低风险 Rust 变更仍保持相对轻量（只 cargo check）。  
  </done>
</task>

<task type="auto">
  <name>Task 3: CI Gate 三平台执行 Rust 单测（Ubuntu 安装构建依赖）</name>
  <files>.github/workflows/ci-gate.yml</files>
  <action>
目标：让 PR/main 的 CI Gate 在 Windows/macOS/Ubuntu 均执行 Rust 单测，避免 `cfg(target_os)` 测试缺失。

1) Windows `quality-gate`：
   - 由于 `check:all` 已包含 `test:rust`，无需新增额外 step（保持现有结构即可）

2) `cross-platform-smoke`（macOS/Ubuntu）：
   - 增加 `Setup Rust toolchain`（对齐其他 workflow：`dtolnay/rust-toolchain@stable`）
   - Ubuntu 增加 `Install Linux build dependencies` step（可直接复用 `release-build.yml` 的依赖清单）：
     - `libgtk-3-dev`
     - `libwebkit2gtk-4.1-dev`
     - `libayatana-appindicator3-dev`
     - `librsvg2-dev`
     - `patchelf`
   - 在 smoke gate 中追加执行：
     - `npm run test:rust`

3) 约束：
   - 不移除现有 TypeScript/Vitest/build smoke；Rust 单测是追加门禁
   - Ubuntu 安装依赖需要 `sudo apt-get`（与 release-build 一致）
  </action>
  <verify>
静态自检（不要求本地跑 CI）：确认 `ci-gate.yml` 的 cross-platform-smoke 在两种 OS 下都具备 rust toolchain step，并且 Ubuntu 有依赖安装 step；最终会执行到 `npm run test:rust`。
  </verify>
  <done>
- CI Gate 的 Windows/macOS/Ubuntu 都会执行 Rust 单测，保证三平台回归可见。  
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check:all` 已包含 `npm run test:rust`
- [ ] `.github/workflows/ci-gate.yml` 的 macOS/Ubuntu smoke 也会运行 Rust 单测（Ubuntu 含系统依赖安装）
- [ ] precommit-guard 对高风险 Rust 变更会运行 Rust 单测
</verification>

<success_criteria>
- Phase 4 的 Rust 单测在 CI 与本地门禁链路都真实执行并可稳定阻断回归
</success_criteria>

<output>
After completion, create `.planning/phases/04-rust-terminal-tests/04-03-SUMMARY.md`
</output>

