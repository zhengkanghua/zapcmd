---
phase: 13-b4-layout-sizing-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/launcher/useWindowSizing/calculation.ts
  - src/composables/launcher/useWindowSizing/model.ts
autonomous: true
requirements: [SIZE-04]
must_haves:
  truths:
    - "sizing 的“内容高度”口径明确排除顶部 drag strip（`.shell-drag-strip` / `--ui-top-align-offset`），避免 floor/补齐比较时出现 18px 漂移。"
    - "measured 与 estimated 两条 sizing 路径对 drag strip 的处理一致（不会出现‘测量算进/估算不算’的口径分裂）。"
    - "在触顶（cap）策略下保持稳定：宁可略大，也不出现偶发裁切/跳动。"
  artifacts:
    - path: src/composables/launcher/useWindowSizing/calculation.ts
      provides: "窗口尺寸计算主入口（measured/estimated 一致口径）"
      contains: "drag strip 排除、cap clamp、floor 触发时窗口高度可达"
    - path: src/composables/launcher/useWindowSizing/model.ts
      provides: "sizing 常量口径（显式包含 top align / drag strip）"
      contains: "drag strip 高度常量或测量策略的输入字段"
  key_links:
    - from: src/composables/launcher/useWindowSizing/calculation.ts
      to: src/styles.css
      via: "drag strip 的 CSS 口径（`--ui-top-align-offset`）必须与 JS fallback 常量一致"
      pattern: "--ui-top-align-offset|shell-drag-strip"
---

<objective>
收敛 sizing/content height 的口径：在计算 floor height 与窗口 sizing 的比较/补齐时，顶部 drag strip（`.shell-drag-strip` / `--ui-top-align-offset`）不计入“内容高度”。确保 measured/estimated 两条路径一致，减少 resize 抖动与裁切风险。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-b4-layout-sizing-foundation/13-CONTEXT.md
@.planning/phases/13-b4-layout-sizing-foundation/13-RESEARCH.md

@src/composables/launcher/useWindowSizing/calculation.ts
@src/composables/launcher/useWindowSizing/model.ts
@src/composables/launcher/useLauncherLayoutMetrics.ts
@src/components/launcher/LauncherWindow.vue
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 显式建模 drag strip 高度，并在 measured/estimated 两条 sizing 路径里统一排除口径</name>
  <files>src/composables/launcher/useWindowSizing/calculation.ts, src/composables/launcher/useWindowSizing/model.ts</files>
  <action>
对 `resolveWindowSize()` 的高度计算做口径收敛（不引入行为回退/吞错）：
1) 明确 drag strip 高度来源（优先 DOM 测量 `.shell-drag-strip` 的 `getBoundingClientRect().height`；无 DOM 时 fallback 到常量，且常量必须与 `--ui-top-align-offset` 同步）；  
2) 引入“内容高度（不含 drag strip）”的中间变量，让 floor/补齐比较基于内容高度进行；最终返回的 window height 仍必须包含 drag strip（否则会裁切）；  
3) measured 与 estimated 都使用同一个 drag strip 口径（同一函数/同一常量输入），避免分裂；  
4) cap clamp 以“内容高度 cap（不含 drag strip）”为基准进行，再加回 drag strip，保证触顶策略稳定一致。
  </action>
  <verify>
1) 运行 `npm run test:run -- src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts`（确保 floor 指标变化未破坏 sizing 调用链）；  
2) 手动：打开 staging（`stagingExpanded=true`）并在不同结果数下反复切换，窗口高度不应出现 18px 来回抖动或裁切。  
  </verify>
  <done>
measured/estimated 两条路径对 drag strip 的处理一致；sizing/floor 的比较与补齐在“内容高度（不含 drag strip）”口径下完成。
  </done>
</task>

<task type="auto">
  <name>Task 2: 确认 floor 触发时 window sizing 能可靠补齐，并保持“稳定优先”策略</name>
  <files>src/composables/launcher/useWindowSizing/calculation.ts</files>
  <action>
在不引入动效的前提下，确保 floor 与 sizing 的联动稳定：
1) 当 `stagingExpanded` opening/open 且结果 0~3 时，`drawerViewportHeight`（含 floor）会推动 `resolveWindowSize()` 的 estimatedHeight 增大（在 measuredHeight 不可用时也成立）；  
2) 若 floor 目标高度 > cap：保持窗口到 cap，drawer 内部滚动承接（不继续拉高窗口）；  
3) 任何边界下都不把 drag strip 计入内容高度比较（避免 floor 少补 18px 或多补 18px）。
  </action>
  <verify>
1) 手动：将屏幕高度调小/窗口 cap 触发（或模拟）时，窗口高度停止增长，drawer 出现内部滚动且内容不裁切；  
2) 验证拖拽区仍可用（drag strip 仍存在且高度正常）。  
  </verify>
  <done>
floor 与 sizing 在 cap 约束下行为稳定：补齐逻辑正确、触顶后内部滚动承接、无 18px 漂移。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] drag strip 不计入“内容高度”比较/补齐口径，但最终 window height 仍包含 drag strip
- [ ] measured/estimated 路径口径一致
- [ ] cap 触顶后内部滚动承接，避免继续拉高窗口
</verification>

<success_criteria>
完成后，Phase 13 的 sizing 口径底座已收敛：为后续 Review overlay 的最小可视高度与 resize 稳定性打下基础。
</success_criteria>

<output>
After completion, create `.planning/phases/13-b4-layout-sizing-foundation/13-02-SUMMARY.md`.
</output>
