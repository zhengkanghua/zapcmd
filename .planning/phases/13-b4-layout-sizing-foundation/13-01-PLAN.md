---
phase: 13-b4-layout-sizing-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/launcher/useLauncherLayoutMetrics.ts
  - src/composables/app/useAppCompositionRoot/viewModel.ts
  - src/App.vue
  - src/components/launcher/LauncherWindow.vue
  - src/components/launcher/types.ts
  - src/components/launcher/parts/LauncherSearchPanel.vue
  - src/styles.css
autonomous: true
requirements: [SIZE-01]
must_haves:
  truths:
    - "搜索态（`stagingExpanded=false`）保持动态矮窗：结果 0~3 条时不会被强制补高。"
    - "当 `stagingExpanded` 进入 opening/open（作为 Review-open 代理）且结果数为 0~3 时，左侧 drawer 会补齐到 floor（4 rows + 搜索框高度），且补高只来自 filler/spacer（不新增假结果条目）。"
    - "filler/spacer 不可聚焦、不可读（`aria-hidden`），不会污染可达性语义。"
    - "结果按钮/条目数量严格等于 `filteredResults.length`（无假结果 DOM）。"
  artifacts:
    - path: src/composables/launcher/useLauncherLayoutMetrics.ts
      provides: "drawer floor height 与 filler 高度的计算口径（触发时机受 `stagingExpanded` 约束）"
      contains: "floor rows=4、0~3 结果分支、fillerHeight=target-natural、搜索态不触发"
    - path: src/components/launcher/parts/LauncherSearchPanel.vue
      provides: "drawer 内部的 filler/spacer 渲染与语义约束"
      contains: "filler 元素位于内容底部、aria-hidden、纯空白"
    - path: src/components/launcher/LauncherWindow.vue
      provides: "把 floor/filler props 传递到 SearchPanel"
      contains: "新增 props 并向下透传"
    - path: src/composables/app/useAppCompositionRoot/viewModel.ts
      provides: "将 layoutMetrics 的 floor/filler 指标暴露给 App.vue"
      contains: "新增 viewModel 字段并在 App.vue 绑定"
  key_links:
    - from: src/composables/launcher/useLauncherLayoutMetrics.ts
      to: src/composables/app/useAppCompositionRoot/viewModel.ts
      via: "layoutMetrics 新增的 floor/filler computed"
      pattern: "drawer.*floor|filler"
    - from: src/composables/app/useAppCompositionRoot/viewModel.ts
      to: src/App.vue
      via: "LauncherWindow props 绑定"
      pattern: "drawer.*(filler|floor)"
    - from: src/components/launcher/LauncherWindow.vue
      to: src/components/launcher/parts/LauncherSearchPanel.vue
      via: "SearchPanel 新增 props"
      pattern: "drawer.*(filler|floor)"
---

<objective>
为 Phase 13 落地 floor height 的“可见”底座：当进入 Review（以 `stagingExpanded` 代理）前/时，若结果不足 4 条（含 0 结果空态），左侧 drawer 通过 filler/spacer 补齐到 floor（4 rows + 搜索框高度），且搜索态保持矮窗不被强制补高。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-b4-layout-sizing-foundation/13-CONTEXT.md
@.planning/phases/13-b4-layout-sizing-foundation/13-RESEARCH.md

@src/composables/launcher/useLauncherLayoutMetrics.ts
@src/components/launcher/parts/LauncherSearchPanel.vue
@src/components/launcher/LauncherWindow.vue
@src/components/launcher/types.ts
@src/composables/app/useAppCompositionRoot/viewModel.ts
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 layoutMetrics 中引入 floor rows=4 的口径与 fillerHeight 计算（仅 Review/opening 触发）</name>
  <files>src/composables/launcher/useLauncherLayoutMetrics.ts</files>
  <action>
在 `useLauncherLayoutMetrics` 内新增 floor 指标（命名可自定，但必须清晰可测）：
1) 固化 floor rows=4（并在屏幕高度 cap 下做合理 clamp：`min(4, maxRowsByHeight, DRAWER_DEFAULT_ROWS)`，避免 floor 超出 cap 后引入非必要抖动）；
2) 仅当 `drawerOpen=true` 且 `stagingExpanded=true` 且 `filteredResults.length` 属于 `0~3` 时启用 floor；
3) 产出两个值：`drawerViewportHeight`（floor 时提升到 4 rows 的 viewport height）与 `drawerFillerHeight`（floorTarget - naturalHeight，最小为 0）；
4) 搜索态（`stagingExpanded=false`）不得触发 floor（保持矮窗）。
  </action>
  <verify>
1) 运行 `npm run test:run -- src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts`（先不加新断言也要保持现有用例不回归）；  
2) 手动 spot-check：在 JS 里临时打印（或用 devtools）确认：结果=1 且 `stagingExpanded=false` 时 fillerHeight=0；结果=1 且 `stagingExpanded=true` 时 fillerHeight>0 且 viewportHeight>=4 rows。
  </verify>
  <done>
layoutMetrics 层面已具备“是否触发 floor、floor 目标高度、fillerHeight”三要素，且触发时机严格受 `stagingExpanded` 约束。
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 SearchPanel 渲染 filler/spacer，保证无假结果 DOM 与 aria 语义不污染</name>
  <files>src/components/launcher/parts/LauncherSearchPanel.vue, src/components/launcher/types.ts, src/components/launcher/LauncherWindow.vue, src/composables/app/useAppCompositionRoot/viewModel.ts, src/App.vue, src/styles.css</files>
  <action>
将 floor/filler 从 layoutMetrics 串到 UI：
1) 新增 props（建议 `drawerFillerHeight` 或等价命名）并贯穿：`viewModel.ts` → `App.vue` → `LauncherWindow.vue` → `LauncherSearchPanel.vue`；
2) 在 `.result-drawer` 内容末尾追加一个纯展示 filler 元素（例如 `div`），仅当 `drawerFillerHeight > 0` 时渲染，并设置：
   - `aria-hidden=\"true\"`
   - 不可聚焦（无 `tabindex`），无 role
   - 样式为纯空白（不引入背景/边框），放在内容底部
   - 高度来自 `drawerFillerHeight`（或用 flex 填充也可，但必须不引入假结果条目）
3) 严禁把 filler 塞进 `<ul>` 里；结果条目仍严格由 `filteredResults` 驱动，保证 `.result-item` 数量= `filteredResults.length`；
4) 若需 CSS：新增一个轻量 class（如 `.result-drawer__filler`）只做布局需要（不要影响交互/可达性）。
  </action>
  <verify>
1) 手动：输入 query 产生 1 条结果 → 关闭 staging（矮窗）→ 点击 queue chip 打开 staging（应明显补高到 floor）；  
2) 通过 DOM 计数验证“无假结果”：检查 `.result-item` 数量严格等于结果数；  
3) `Tab` 导航不会进入 filler；屏幕阅读器语义不读 filler（`aria-hidden`）。  
  </verify>
  <done>
floor 的“空白补高”只由 filler/spacer 提供；结果 DOM 不被污染；搜索态与 Review 态的触发时机符合 Context。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 搜索态不触发 floor（0~3 结果仍可矮）
- [ ] `stagingExpanded` opening/open 且 0~3 结果触发 floor，并通过 filler/spacer 补齐
- [ ] filler `aria-hidden` 且不可聚焦；无假结果 DOM
</verification>

<success_criteria>
本计划完成后，Phase 13 的 floor height 视觉与语义约束已经落地：为后续 Phase 14 的 Review overlay 最小可视高度对齐提供稳定底座。
</success_criteria>

<output>
After completion, create `.planning/phases/13-b4-layout-sizing-foundation/13-01-SUMMARY.md`.
</output>

