---
phase: 13-b4-layout-sizing-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts
  - src/composables/__tests__/launcher/useWindowSizing.calculation.test.ts
  - src/components/launcher/parts/__tests__/LauncherSearchPanel.floor-height.test.ts
autonomous: true
requirements: [TST-02, SIZE-01, SIZE-04]
must_haves:
  truths:
    - "单测覆盖 floor 关键分支：结果数 `0/1/3/4` ×（`stagingExpanded=false` vs `true`），并断言 floor 仅在 `stagingExpanded=true` 时触发。"
    - "单测覆盖 drag strip 排除口径：不把 `.shell-drag-strip` 高度计入内容高度比较（失败输出包含关键数值，便于定位）。"
    - "组件层断言“无假结果 DOM”：`.result-item` 数量严格等于 `filteredResults.length`，filler `aria-hidden` 且不可聚焦。"
  artifacts:
    - path: src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts
      provides: "floor/filler 计算的单测回归"
      contains: "0/1/3/4 结果覆盖与触发时机断言"
    - path: src/composables/__tests__/launcher/useWindowSizing.calculation.test.ts
      provides: "sizing 口径（drag strip 排除、measured/estimated 一致性）的单测回归"
      contains: "debug 输出、cap 边界、18px 漂移防护"
    - path: src/components/launcher/parts/__tests__/LauncherSearchPanel.floor-height.test.ts
      provides: "SearchPanel 的 DOM 语义回归"
      contains: "无假结果 DOM、filler aria-hidden、位置在内容底部"
  key_links:
    - from: src/composables/__tests__/launcher/useWindowSizing.calculation.test.ts
      to: src/composables/launcher/useWindowSizing/calculation.ts
      via: "针对 drag strip 排除与 floor 联动的断言"
      pattern: "drag strip|top align|floor"
---

<objective>
把 Phase 13 的关键分支锁进可定位单测：floor（0/1/3/4 结果、触发时机）、drag strip 排除口径（measured/estimated 一致）、以及“无假结果 DOM”的组件语义约束。测试失败时必须直接暴露关键数值，避免肉眼判断。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-b4-layout-sizing-foundation/13-CONTEXT.md
@.planning/phases/13-b4-layout-sizing-foundation/13-RESEARCH.md

@src/composables/launcher/useLauncherLayoutMetrics.ts
@src/composables/launcher/useWindowSizing/calculation.ts
@src/components/launcher/parts/LauncherSearchPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 layoutMetrics 单测，锁定 floor 触发与 fillerHeight（0/1/3/4 × stagingExpanded）</name>
  <files>src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts</files>
  <action>
在现有 `useLauncherLayoutMetrics.test.ts` 基础上新增断言（避免写死具体 px，优先断言“由 4 rows + search-form 口径推导”的相对关系）：
1) `stagingExpanded=false` 时：结果为 0/1/3 时 `drawerFillerHeight` 必须为 0（不触发 floor）；  
2) `stagingExpanded=true` 时：结果为 0/1/3 时 `drawerFillerHeight` 必须 > 0，且 `drawerViewportHeight` >= 4 rows 的 viewport height；  
3) 结果为 4 时：`drawerFillerHeight` 必须为 0（floor 不应额外补齐）；  
4) 失败输出要可定位：在断言 message 中输出 `{ resultCount, stagingExpanded, drawerViewportHeight, fillerHeight }`。
  </action>
  <verify>
运行 `npm run test:run -- src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts` 并确认新增断言稳定通过。
  </verify>
  <done>
floor 的触发时机与 0/1/3/4 结果分支被单测锁定，失败可直接定位关键数值。
  </done>
</task>

<task type="auto">
  <name>Task 2: 新增 window sizing 单测，锁定 drag strip 排除口径与 measured/estimated 一致性</name>
  <files>src/composables/__tests__/launcher/useWindowSizing.calculation.test.ts</files>
  <action>
新增 `useWindowSizing.calculation.test.ts`（或等价命名），覆盖：
1) estimated 路径：当 `searchShellRef=null` 时走 estimate，断言 drag strip 不计入内容高度比较（可通过设置 `constants` 中 drag strip 高度并对比输出高度的差值来证明）；  
2) measured 路径：构造 DOM 元素并 stub `getBoundingClientRect()`（含 `.shell-drag-strip` 高度），断言与 estimated 得到一致口径；  
3) cap 边界：当 floor 目标 > cap 时，最终 height=cap（或等价 clamp 逻辑），并解释为“内部滚动承接”；  
4) 失败输出可定位：断言 message 输出 `{ dragStripHeight, contentHeight, windowHeight, cap }`（具体字段按实现调整，但必须包含 strip 与 cap 两个关键量）。
  </action>
  <verify>
运行 `npm run test:run -- src/composables/__tests__/launcher/useWindowSizing.calculation.test.ts`。
  </verify>
  <done>
drag strip 排除口径被单测锁定，且 measured/estimated 不再出现 18px 漂移分裂。
  </done>
</task>

<task type="auto">
  <name>Task 3: 新增 SearchPanel 组件语义测试，证明“无假结果 DOM + filler aria-hidden”</name>
  <files>src/components/launcher/parts/__tests__/LauncherSearchPanel.floor-height.test.ts</files>
  <action>
新增组件测试（使用 `@vue/test-utils` mount）：
1) 传入 `filteredResults` 为 0/1/3/4，断言 `.result-item` 的数量严格等于 `filteredResults.length`；  
2) 当 `drawerFillerHeight > 0` 时：filler 元素存在、位于 drawer 内容底部、`aria-hidden=\"true\"`，且不包含可聚焦元素；  
3) 当 `drawerFillerHeight = 0` 时：filler 元素不应渲染（避免污染 DOM）。  
  </action>
  <verify>
运行 `npm run test:run -- src/components/launcher/parts/__tests__/LauncherSearchPanel.floor-height.test.ts`。
  </verify>
  <done>
“无假结果 DOM”与 filler 语义约束具备稳定自动化回归，失败输出可定位。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] floor 的 0/1/3/4 × stagingExpanded 分支有单测断言
- [ ] drag strip 排除口径与 cap 边界有单测断言
- [ ] SearchPanel 无假结果 DOM + filler aria 语义有组件单测断言
</verification>

<success_criteria>
Phase 13 的 sizing/floor 关键分支被自动化回归锁定，且断言失败可定位，满足 TST-02 与 Phase 13 success criteria。
</success_criteria>

<output>
After completion, create `.planning/phases/13-b4-layout-sizing-foundation/13-03-SUMMARY.md`.
</output>

