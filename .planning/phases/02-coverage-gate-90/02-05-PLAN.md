---
phase: 02-coverage-gate-90
plan: "05"
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - vitest.config.ts
  - src/stores/__tests__/settingsStore.test.ts
  - src/features/security/__tests__/commandSafety.test.ts
  - src/composables/update/useUpdateManager.ts
  - src/composables/__tests__/update/useUpdateManager.test.ts
autonomous: true
requirements: [COV-01]
must_haves:
    truths:
      - "`npm run test:coverage` 在本地与 CI 中通过，且 thresholds 提升到 lines/functions/statements/branches 四项 ≥90%。"
      - "覆盖率提升不通过大范围 exclude 达成；如需最小范围 exclude（例如纯类型文件异常），必须在变更中明确记录理由与范围。"
    artifacts:
      - path: "vitest.config.ts"
        provides: "coverage thresholds: 90/90/90/90"
        contains: "thresholds"
      - path: "src/stores/__tests__/settingsStore.test.ts"
        provides: "settingsStore 分支缺口补齐用例"
        contains: "migrateSettingsPayload"
      - path: "src/composables/__tests__/update/useUpdateManager.test.ts"
        provides: "update manager 分支覆盖补齐用例"
        contains: "useUpdateManager"
---

<objective>
收尾 Phase 2（COV-01）：补齐剩余 branch 缺口、将 thresholds 一次性提升到 90%，并锁定在本地与 CI 均通过。

重点：
- 继续补齐 `settingsStore.ts` 等剩余热点分支缺口
- 必要时补齐 `commandSafety.ts` / `updateService.ts` / `useUpdateManager.ts` 等 Top deficits
- 最终将 `vitest.config.ts` thresholds 提升到 90/90/90/90
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-coverage-gate-90/02-CONTEXT.md
@.planning/phases/02-coverage-gate-90/02-RESEARCH.md

@vitest.config.ts
@src/stores/settingsStore.ts
@src/stores/__tests__/settingsStore.test.ts
@src/features/security/commandSafety.ts
@src/features/security/__tests__/commandSafety.test.ts
@src/composables/update/useUpdateManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补齐 settingsStore 的未覆盖分支（迁移/归一化/边界）</name>
  <files>src/stores/__tests__/settingsStore.test.ts</files>
  <action>
根据 coverage 报告补齐 `settingsStore.ts` 的未覆盖分支，优先覆盖：
- normalizeBoolean 的 string/number 等边界分支
- normalizeWindowOpacity 的 min/max/NaN 边界
- setCommandEnabled 空 id 早退
- migrateSettingsPayload 在部分字段缺失/非法类型时的兜底路径

要求：新增用例应围绕“一个分支一个断言”编写，失败信息可读。
  </action>
  <verify>
运行 `npm run test:coverage`，确认 settingsStore 相关缺口下降，All files branches 向 90% 靠近。
  </verify>
  <done>
- settingsStore 的关键边界分支被补齐覆盖。
  </done>
</task>

<task type="auto">
  <name>Task 2: 针对剩余 Top deficits 补齐测试（必要时新增 useUpdateManager 单测）</name>
  <files>src/features/security/__tests__/commandSafety.test.ts, src/composables/update/useUpdateManager.ts, src/composables/__tests__/update/useUpdateManager.test.ts</files>
  <action>
使用 `npm run test:coverage` 的 Top deficits 输出（或 `coverage/index.html`）定位剩余短板：
- 若 `commandSafety.ts` / `updateService.ts` / `useUpdateManager.ts` 等仍在 Top deficits 中，补齐关键分支单测
- `useUpdateManager.ts` 当前覆盖较低：允许新增一个最小单测文件 `src/composables/__tests__/update/useUpdateManager.test.ts`，通过 mock/stub 隔离外部依赖，仅覆盖核心分支

目标：在不改 thresholds 的情况下，先让 All files 四项覆盖率 **实际达到 ≥90%**（用 coverage 报告核对）。
  </action>
  <verify>
运行 `npm run test:coverage`，确认 `coverage/index.html` 的 All files 四项覆盖率都 ≥90%（或 wrapper 输出显示已达标）。
  </verify>
  <done>
- 90% 目标已在“真实覆盖率”层面达成，为阈值提升做准备。
  </done>
</task>

<task type="auto">
  <name>Task 3: 提升 vitest thresholds 到 90/90/90/90 并锁定门禁</name>
  <files>vitest.config.ts</files>
  <action>
将 `vitest.config.ts` 的 coverage thresholds 调整为：
- lines: 90
- functions: 90
- statements: 90
- branches: 90

要求：
- 不新增大范围 exclude；如确需最小范围 exclude（例如纯类型文件导致报告异常），必须在改动中解释理由与范围，并确保范围最小。
  </action>
  <verify>
1) `npm run test:coverage` 通过  
2) `npm run check:all` 通过（确保门禁链路全绿）
  </verify>
  <done>
- Phase 2 的覆盖率门禁正式提升到 90% 并能稳定通过。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:coverage` 在 thresholds=90/90/90/90 下通过
- [ ] `coverage/index.html`（All files）四项覆盖率均 ≥90%
- [ ] 未通过大范围 exclude “刷指标”
</verification>

<success_criteria>
- 覆盖率门禁提升到 90%（COV-01）并且失败信息可定位（配合 02-01）
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-gate-90/02-05-SUMMARY.md`
</output>

