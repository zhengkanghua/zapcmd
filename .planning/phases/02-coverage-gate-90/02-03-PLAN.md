---
phase: 02-coverage-gate-90
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/composables/__tests__/launcher/useLauncherSessionState.test.ts
autonomous: true
requirements: [COV-01]
must_haves:
    truths:
      - "`src/composables/launcher/useLauncherSessionState.ts` 的 branches 覆盖率提升到 ≥90%（重点覆盖 sanitize/normalize 与存储读写分支）。"
      - "新增测试覆盖：版本不匹配、字段缺失、args/argValues 异常、adminRequired/dangerous 等分支。"
    artifacts:
      - path: "src/composables/__tests__/launcher/useLauncherSessionState.test.ts"
        provides: "launcher session state 的边界/异常分支覆盖用例"
        contains: "useLauncherSessionState"
---

<objective>
补齐 `useLauncherSessionState.ts` 的 sanitize/normalize 分支覆盖（该文件缺失分支较多，且可通过依赖注入 storage 完整单测覆盖）。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-coverage-gate-90/02-CONTEXT.md
@.planning/phases/02-coverage-gate-90/02-RESEARCH.md

@src/composables/launcher/useLauncherSessionState.ts
@src/composables/__tests__/launcher/useLauncherSessionState.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 覆盖 restore 读取路径的边界/异常分支（version、payload 结构、stagedCommands）</name>
  <files>src/composables/__tests__/launcher/useLauncherSessionState.test.ts</files>
  <action>
在现有测试基础上补齐以下分支覆盖：
- snapshot version 不匹配（应清理 storage 并不崩溃）
- stagedCommands 非数组 / 数组中存在非法项（缺字段/空字符串）应被过滤
- stagingExpanded 为 truthy/falsey 的差异路径（drawer 是否打开）

建议做法：
- 继续使用注入的 `storage` mock
- 通过构造不同 raw snapshot payload 覆盖 normalizeSessionPayload / sanitizeStagedCommand 的分支
  </action>
  <verify>
运行 `npm run test:coverage`，确认该文件的 branches 缺口明显减少。
  </verify>
  <done>
- restore 路径在多种异常 payload 下都能安全降级，不写入脏状态。
  </done>
</task>

<task type="auto">
  <name>Task 2: 覆盖 args/argValues sanitize 与 adminRequired/dangerous 等分支</name>
  <files>src/composables/__tests__/launcher/useLauncherSessionState.test.ts</files>
  <action>
补齐以下分支：
- `sanitizeArg()`：key/label/token 缺失；placeholder/required/defaultValue/argType/validation* 分支；options 过滤（空串/非 string）
- `sanitizeStagedCommand()`：argValues 非对象；args 含非法项；adminRequired/dangerous boolean 分支

目标：将 `useLauncherSessionState.ts` 的 branches 提升到 ≥90%。
  </action>
  <verify>
运行 `npm run test:coverage`，检查 `useLauncherSessionState.ts` branches ≥90%。
  </verify>
  <done>
- sanitize/normalize 的关键分支被单测覆盖，branches 达标。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:coverage` 通过（在现有 thresholds 下）
- [ ] `useLauncherSessionState.ts` branches ≥90%
</verification>

<success_criteria>
- launcher session state 的边界/异常分支覆盖补齐，为整体 branches ≥90% 贡献增量
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-gate-90/02-03-SUMMARY.md`
</output>

