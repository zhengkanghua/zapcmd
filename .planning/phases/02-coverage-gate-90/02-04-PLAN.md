---
phase: 02-coverage-gate-90
plan: "04"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/composables/__tests__/settings/useCommandManagement.test.ts
autonomous: true
requirements: [COV-01]
must_haves:
    truths:
      - "`src/composables/settings/useCommandManagement.ts` 的 branches 覆盖率提升到 ≥90%（重点覆盖 formatIssue / compareRows / 多维过滤器）。"
      - "新增测试覆盖所有 issue code 分支与主要 sort/filter 组合，不依赖真实 UI。"
    artifacts:
      - path: "src/composables/__tests__/settings/useCommandManagement.test.ts"
        provides: "command management 的筛选/排序/issue 分支覆盖用例"
        contains: "useCommandManagement"
---

<objective>
补齐 `useCommandManagement.ts` 的筛选/排序/issue 格式化分支覆盖，提升 branches 并为后续 90% 门禁打底。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-coverage-gate-90/02-CONTEXT.md
@.planning/phases/02-coverage-gate-90/02-RESEARCH.md

@src/composables/settings/useCommandManagement.ts
@src/composables/__tests__/settings/useCommandManagement.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 覆盖 formatIssue 与 compareRows 的主要分支</name>
  <files>src/composables/__tests__/settings/useCommandManagement.test.ts</files>
  <action>
扩展测试覆盖：
- `formatIssue()` 的所有分支：
  - invalid-json
  - invalid-schema
  - duplicate-id（含 commandId 为 undefined 的兜底）
  - 默认分支（shell-ignored）
- `compareRows()` 的 sortBy 分支：
  - title / category / source / status / default
  - 覆盖 enabled/disabled、overridesBuiltin 的排序差异

要求：尽量用最小数据集验证排序输出，断言明确。
  </action>
  <verify>
运行 `npm run test:coverage`，确认 branches 缺口减少。
  </verify>
  <done>
- issue 格式化与排序策略的主要分支被单测覆盖。
  </done>
</task>

<task type="auto">
  <name>Task 2: 覆盖过滤器组合与 bulk enable/disable 的边界分支</name>
  <files>src/composables/__tests__/settings/useCommandManagement.test.ts</files>
  <action>
补齐以下分支：
- query/source/status/override/issue/fileFilter 的组合过滤（每条用例只验证一个维度，避免用例过大）
- displayMode=groupedByFile 的分组逻辑（unknown source 的 title 兜底）
- `setFilteredCommandsEnabled()`：
  - 过滤结果为空时提前 return
  - enabled=true/false 两条路径

目标：将 `useCommandManagement.ts` 的 branches 提升到 ≥90%。
  </action>
  <verify>
运行 `npm run test:coverage`，检查 `useCommandManagement.ts` branches ≥90%。
  </verify>
  <done>
- command management 的筛选/分组/bulk 操作关键分支覆盖补齐。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:coverage` 通过（在现有 thresholds 下）
- [ ] `useCommandManagement.ts` branches ≥90%
</verification>

<success_criteria>
- command management 的分支覆盖补齐，为整体 branches ≥90% 提供关键增量
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-gate-90/02-04-SUMMARY.md`
</output>

