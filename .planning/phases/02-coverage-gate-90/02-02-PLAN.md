---
phase: 02-coverage-gate-90
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/features/commands/__tests__/schemaGuard.test.ts
autonomous: true
requirements: [COV-01]
must_haves:
    truths:
      - "`src/features/commands/schemaGuard.ts` 的 branches 覆盖率显著提升（目标：≥90%），为整体 branches ≥90% 提供主要增量。"
      - "新增/补齐的测试用例以表驱动方式覆盖多种 invalid payload（每条用例尽量只触发一个校验分支，便于定位）。"
    artifacts:
      - path: "src/features/commands/__tests__/schemaGuard.test.ts"
        provides: "schemaGuard 的分支覆盖提升用例集"
        contains: "isRuntimeCommandFile"
    key_links:
      - from: "src/features/commands/runtimeLoader.ts"
        to: "src/features/commands/schemaGuard.ts"
        via: "isRuntimeCommandFile"
        pattern: "isRuntimeCommandFile"
---

<objective>
提升 Phase 2 的核心短板：branches 覆盖率。

优先补齐 `schemaGuard.ts` 的分支覆盖：该文件是纯校验逻辑、分支多且可用数据驱动测试快速覆盖，是把 branches 从 ~82% 拉升到 90% 的最优 ROI 目标之一。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-coverage-gate-90/02-CONTEXT.md
@.planning/phases/02-coverage-gate-90/02-RESEARCH.md

@src/features/commands/schemaGuard.ts
@src/features/commands/__tests__/schemaGuard.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 schemaGuard 的 invalid payload 表驱动测试（top-level / meta / command 基础字段）</name>
  <files>src/features/commands/__tests__/schemaGuard.test.ts</files>
  <action>
在现有测试基础上扩展“表驱动 invalid cases”，覆盖至少以下分支：
- top-level：非 object、包含未知 key、commands 非数组/为空
- `_meta`：非 object；字段为非法类型/空字符串；localized text 对象为空/含空 key/空 value
- command：id 非法（空/不匹配 pattern）、name 非法、tags 非法（空/重复/含空串）、category/platform/shell 非法枚举、adminRequired 非 boolean、description 非法

要求：
- 每个 invalid case 尽量只让一个关键字段变坏，减少“多分支同时命中”的噪音
- 用例命名清晰（说明触发哪条校验）
  </action>
  <verify>
运行 `npm run test:coverage`，确认 `schemaGuard.ts` 的 branches 明显上升（可通过 `coverage/index.html` 或 `test:coverage` 的 Top deficits 输出观察缺口减少）。
  </verify>
  <done>
- 覆盖了 schemaGuard 的核心基础校验分支（top-level/meta/command 基础字段）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 覆盖 args / prerequisites / select-options 等深层校验分支</name>
  <files>src/features/commands/__tests__/schemaGuard.test.ts</files>
  <action>
继续扩展用例，覆盖至少以下分支：
- args：key pattern、label localized/object、type 枚举、required/default/placeholder 类型、validation 结构合法性
- `type === select`：必须存在 validation.options、options 必须为非空且去重
- prerequisites：type 枚举、required 类型、check 非空、installHint localized/object、fallbackCommandId pattern

目标：将 `schemaGuard.ts` 的 branches 覆盖率提升到 **≥90%**（尽量接近 100%，但优先保证用例可读与可维护）。
  </action>
  <verify>
运行 `npm run test:coverage`，检查 `schemaGuard.ts` branches ≥90%。
  </verify>
  <done>
- schemaGuard 深层校验分支覆盖齐全，为整体 branches ≥90% 提供主要增量。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:coverage` 通过（在现有 thresholds 下）
- [ ] `schemaGuard.ts` branches ≥90%（以 coverage 报告为准）
</verification>

<success_criteria>
- schemaGuard 的分支缺口大幅收敛，为 Phase 2 的 branches ≥90% 目标提供关键提升
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-gate-90/02-02-SUMMARY.md`
</output>

