---
phase: 14-review-overlay
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/launcher/LauncherWindow.vue
  - src/components/launcher/types.ts
  - src/components/launcher/parts/LauncherReviewOverlay.vue
  - src/components/launcher/parts/LauncherStagingPanel.vue
  - src/composables/app/useAppCompositionRoot/runtime.ts
  - src/composables/launcher/useStagingQueue/guards.ts
  - src/composables/launcher/useLauncherLayoutMetrics.ts
  - src/composables/launcher/useWindowSizing/calculation.ts
  - src/styles.css
autonomous: true
requirements: [SHELL-02, SHELL-03, SIZE-02, REV-02, REV-03]
must_haves:
  truths:
    - "Review 打开后背景仍可见，但不可交互：不可点击、不可滚动、不可获得焦点（当前唯一可交互层为 Review）。"
    - "点击 dim/scrim 区域只会关闭 Review（不会触发 blank click hide 主窗）。"
    - "顶部拖拽区在 Review 打开时仍可用：overlay/scrim 不覆盖 `.shell-drag-strip`，且不吞命中。"
    - "Review 面板内部列表在面板内滚动：队列项变多时不会持续拉高窗口；最小可视高度与 drawer floor height 对齐。"
    - "Review 列表的长命令以摘要呈现：默认不铺完整长命令；可通过 tooltip/title 查看完整；提供复制按钮。"
    - "队列能力不回归：删除、拖拽排序、清空、执行全部仍可用；队列清空时保持在 Review 内显示空态并可关闭。"
  artifacts:
    - path: src/components/launcher/parts/LauncherReviewOverlay.vue
      provides: "B4 Review overlay 主交互层（含 scrim + panel + 关闭入口）"
      contains: "data-hit-zone='overlay'、dialog 语义、drag strip 不覆盖、内部滚动结构"
    - path: src/composables/app/useAppCompositionRoot/runtime.ts
      provides: "背景锁定：阻断 search input 聚焦（shouldBlockSearchInputFocusRef 包含 Review 打开态）"
      contains: "stagingExpanded/reviewOpen 参与 shouldBlockSearchInputFocusRef"
    - path: src/composables/launcher/useStagingQueue/guards.ts
      provides: "空队列时不强制把焦点送回搜索（Review 仍可保持打开）"
      contains: "length===0 分支在 Review 打开时不 scheduleSearchInputFocus"
  key_links:
    - from: src/components/launcher/LauncherWindow.vue
      to: src/composables/launcher/useLauncherHitZones.ts
      via: "overlay/scrim 标记 data-hit-zone='overlay'，避免 blank click hide"
      pattern: "data-hit-zone=\"overlay\"|blank-pointerdown"
    - from: src/components/launcher/parts/LauncherReviewOverlay.vue
      to: src/composables/launcher/useWindowSizing/calculation.ts
      via: "复用 stagingPanelRef 作为 overlay panel 的测量锚点，确保窗口不裁切"
      pattern: "setStagingPanelRef|stagingPanelRef"
---

<objective>
把队列 UI 从旧 staging 侧栏迁移为 **B4 Review overlay**：建立背景锁定（可见但不可交互）、scrim/dim 关闭语义、drag strip 不被吞；同时在 Review 内实现“列表内部滚动 + 命令摘要 + 复制入口”，并确保删除/排序/清空/执行等队列能力不回归。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-review-overlay/14-CONTEXT.md
@.planning/phases/14-review-overlay/14-RESEARCH.md

@docs/ui-redesign/08-b4-interaction-state-machine.md
@docs/ui-redesign/10-b4-component-architecture.md
@docs/ui-redesign/11-b4-visual-spec.md
@docs/ui-redesign/12-b4-acceptance-matrix.md

@src/components/launcher/LauncherWindow.vue
@src/components/launcher/types.ts
@src/components/launcher/parts/LauncherStagingPanel.vue
@src/composables/launcher/useLauncherHitZones.ts
@src/composables/launcher/useStagingQueue/guards.ts
@src/composables/app/useAppCompositionRoot/runtime.ts
@src/composables/execution/useCommandExecution/helpers.ts
@src/composables/launcher/useLauncherLayoutMetrics.ts
@src/composables/launcher/useWindowSizing/calculation.ts
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 引入 `LauncherReviewOverlay.vue`（scrim + panel），并在 LauncherWindow 挂载为 overlay 层</name>
  <files>src/components/launcher/parts/LauncherReviewOverlay.vue, src/components/launcher/LauncherWindow.vue, src/components/launcher/types.ts, src/styles.css</files>
  <action>
1) 新建 `LauncherReviewOverlay.vue`（推荐策略：新文件实现 overlay，旧 `LauncherStagingPanel.vue` 暂留作参考，避免边改边缠绕）。\n
2) overlay 结构必须包含：\n
   - scrim/dim 背景层（覆盖 search 区域，但 **不覆盖** `.shell-drag-strip` 所在区域）；\n
   - review panel（宽度使用 Plan 14-01 建立的 CSS 变量，如 `--review-width`）；\n
   - 明确的关闭按钮（同时支持点击 scrim 关闭）。\n
3) hit-zones：scrim 与 panel 根节点必须带 `data-hit-zone=\"overlay\"`，防止触发 `blank-pointerdown` 导致主窗隐藏；scrim 点击只做 `closeReview()`。\n
4) 语义建议：panel 使用 `role=\"dialog\"` + `aria-modal=\"true\"`（若与既有 overlays 冲突，以不破坏 Param/Safety overlay 为先）。\n
5) 在 `LauncherWindow.vue`：当 `stagingExpanded`（本阶段仍复用该状态）为 true 时挂载 `LauncherReviewOverlay`；并移除/停止使用旧并列 `LauncherStagingPanel` 的常驻列渲染（实现 “Review 是 overlay，不是常驻并列栏”）。\n
6) 复用现有 ref 通道：将 `setStagingPanelRef` 传给 Review overlay 的 panel 根元素（让 `useWindowSizing` 仍能测量到 overlay，避免裁切）。\n
7) CSS：scrim 的 dim 建议使用命名变量统一管理（参考 `docs/ui-redesign/11-b4-visual-spec.md` 的 alpha 区间），并确保拖拽区仍在最上层可命中。
  </action>
  <verify>
手动用例（见 `docs/ui-redesign/12-b4-acceptance-matrix.md`）：\n
1) 打开 Review → scrim 出现但不遮 drag strip（可拖动窗口）；\n
2) 点击 scrim → 关闭 Review（主窗不隐藏）。\n
  </verify>
  <done>
Review overlay 的壳层（scrim + panel + 关闭语义）落地，且 hit-zone 与 drag strip 约束满足。
  </done>
</task>

<task type="auto">
  <name>Task 2: 迁移/实现 Review 内部队列 UI（摘要 + 复制 + 内部滚动 + 队列动作不回归）</name>
  <files>src/components/launcher/parts/LauncherReviewOverlay.vue, src/components/launcher/parts/LauncherStagingPanel.vue, src/styles.css, src/composables/launcher/useLauncherLayoutMetrics.ts</files>
  <action>
在 `LauncherReviewOverlay.vue` 内实现 Review 的 header/list/footer（可直接复用 `LauncherStagingPanel.vue` 的渲染结构与事件模型，但输出必须符合 B4 规格）：\n
1) 列表滚动：header/footer 固定，列表容器 `overflow: auto`，并由 layoutMetrics 提供 maxHeight / minHeight，使队列项增多时不再推高窗口（满足 `SIZE-02`）。\n
   - 最小可视高度对齐 drawer floor：优先复用 `useLauncherLayoutMetrics.drawerFloorViewportHeight`（或等价指标）来计算 Review panel 的内容区 minHeight。\n
2) 命令摘要：主列表不再直接渲染完整 `cmd.renderedCommand`。\n
   - 建议复用 `summarizeCommandForFeedback()`（`src/composables/execution/useCommandExecution/helpers.ts`）或实现 UI 专用摘要函数（压缩空白 + 截断）；\n
   - 完整命令通过 `title` tooltip 或明确的“查看完整”方式提供，但默认列表保持摘要。\n
3) 复制按钮：每条卡片提供“复制命令”按钮，将完整命令写入剪贴板；失败要显性可见（console error 或 toast），禁止静默吞错。\n
4) 队列能力不回归：\n
   - 删除：沿用 `remove-staged-command`；\n
   - 排序：沿用 drag 事件（`staging-drag-*`）与 active index；\n
   - 清空 / 执行全部：沿用 `clear-staging` / `execute-staged`。\n
5) 空队列：Review 打开后若队列被清空，仍停留在 Review 内显示空态文案与关闭入口（不自动关闭）。\n
6) 参数输入：保持 staging 卡片中的 inline 参数输入能力（复用既有事件 `update-staged-arg`）。
  </action>
  <verify>
手动：准备 10+ 条队列项 → 打开 Review：\n
- 列表在 panel 内滚动，窗口高度不持续增长；\n
- 长命令默认展示摘要，且可复制完整命令；\n
- 删除/排序/清空/执行全部均可用；\n
- 清空后仍在 Review 内显示空态。
  </verify>
  <done>
Review 内部交付：滚动结构、长命令摘要策略、复制入口与队列动作能力均符合需求。
  </done>
</task>

<task type="auto">
  <name>Task 3: 背景锁定（点击/滚动/聚焦）收口，并确保滚轮在背景区域驱动 Review 列表滚动</name>
  <files>src/components/launcher/parts/LauncherReviewOverlay.vue, src/components/launcher/parts/LauncherSearchPanel.vue, src/composables/app/useAppCompositionRoot/runtime.ts, src/composables/launcher/useStagingQueue/guards.ts, src/styles.css</files>
  <action>
实现 `SHELL-02` 的“背景不可交互”细则：\n
1) 点击：scrim 覆盖背景交互区，背景不再接收 pointer events；\n
2) 滚动：背景区域滚轮应驱动 Review 列表滚动：在 scrim 上监听 `wheel`，将 `deltaY` 转发到 Review list 容器（必要时 `preventDefault`，但不要影响 trackpad 的自然滚动）；\n
3) 聚焦：阻断 search input 获取焦点：\n
   - `createAppCompositionRuntime` 中 `shouldBlockSearchInputFocusRef` 需要把 `stagingExpanded`（Review 打开态）纳入；\n
   - 背景容器可选加 `inert`（若使用必须确保不影响 drag strip 与 Param/Safety overlays），并补充 `aria-hidden` 策略（不要让屏幕阅读器在 Review 打开时仍读到背景可交互内容）。\n
4) 修复“空队列时 guard 把焦点送回 search”的副作用：调整 `useStagingQueue/guards.ts`，让 `length===0` 时在 Review 打开态不再强制 `focusZone='search' + scheduleSearchInputFocus()`。\n
5) Drag strip：任何锁定/遮罩/样式都不得覆盖 `.shell-drag-strip` 命中与 drag region。
  </action>
  <verify>
手动：打开 Review 后，分别尝试：\n
- 点击背景搜索输入/结果列表：不应获得焦点或触发点击；\n
- 在背景区域滚动鼠标滚轮：应滚动 Review 列表（而不是背景）；\n
- 反复清空队列：Review 保持打开并停留在空态；\n
- 顶部拖拽区仍可拖动窗口。\n
  </verify>
  <done>
Review 打开时背景锁定（点击/滚动/聚焦）成立，滚轮转发行为可用且不破坏 drag strip 与上层阻断层。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Review 打开后背景不可点击/滚动/聚焦（且滚轮转发到 Review 列表）
- [ ] 点击 scrim 关闭 Review，不隐藏主窗
- [ ] drag strip 在 Review 打开时仍可用且不被遮罩吞掉
- [ ] Review 列表内部滚动，窗口不随队列项持续拉高
- [ ] 长命令摘要 + tooltip + 复制入口落地
- [ ] 删除/排序/清空/执行全部等队列能力不回归；清空后保持在 Review
</verification>

<success_criteria>
本计划完成后，Phase 14 的核心交付（Review overlay + 背景锁定 + 更宽面板 + 内部滚动 + 长命令摘要策略）已落地，用户可在 Review 内完成审阅与执行，而搜索层退化为纯上下文背景。
</success_criteria>

<output>
After completion, create `.planning/phases/14-review-overlay/14-02-SUMMARY.md`.
</output>

