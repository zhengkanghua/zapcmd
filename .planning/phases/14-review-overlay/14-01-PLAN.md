---
phase: 14-review-overlay
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/launcher/LauncherWindow.vue
  - src/components/launcher/types.ts
  - src/components/launcher/parts/LauncherSearchPanel.vue
  - src/components/launcher/parts/LauncherQueueSummaryPill.vue
  - src/composables/execution/useCommandExecution/helpers.ts
  - src/composables/launcher/useLauncherSessionState.ts
  - src/composables/launcher/useLauncherLayoutMetrics.ts
  - src/composables/launcher/useWindowSizing/calculation.ts
  - src/styles.css
autonomous: true
requirements: [SHELL-01, REV-01, VIS-03]
must_haves:
  truths:
    - "用户打开 launcher 主窗口时是单焦点搜索态：不再出现常驻并列的右侧 staging 工作区（右侧只在 Review 打开时出现）。"
    - "队列非空时，搜索区出现 queue summary pill（显示数量），并且它是进入 Review 的主要入口之一；队列为空时 pill 不渲染且不占位。"
    - "入队（首次从 0→1）不会自动打开 Review；用户继续保持在搜索态。"
    - "Review 面板宽度口径统一：`reviewWidth = clamp(searchMainWidth * 2/3, 420, 480)`，并以命名常量集中管理（不在组件/CSS 内散落魔法值）。"
    - "长命令的“可读性改进”至少先由宽度提升兜底：Review 宽度达到 `420px~480px` 范围。"
  artifacts:
    - path: src/components/launcher/parts/LauncherQueueSummaryPill.vue
      provides: "搜索态的 queue summary pill（仅队列非空时出现）"
      contains: "count 显示、aria-label、click → 打开 Review（沿用 toggle-staging 事件）"
    - path: src/composables/execution/useCommandExecution/helpers.ts
      provides: "入队行为：不再因 wasEmpty 自动打开 Review"
      contains: "appendToStaging 移除 openStagingDrawer() 的自动触发"
    - path: src/composables/launcher/useLauncherLayoutMetrics.ts
      provides: "Review 宽度计算口径（2/3 + clamp 420~480）并暴露为 CSS 变量"
      contains: "集中常量、computed(reviewWidth)、searchShellStyle 注入 --review-width"
    - path: src/styles.css
      provides: "壳层单焦点化：默认仅单列 Search；Review 宽度使用 CSS 变量"
      contains: ".search-shell 单列基线；Review 打开时不再出现常驻右侧列"
  key_links:
    - from: src/composables/launcher/useLauncherLayoutMetrics.ts
      to: src/styles.css
      via: "Review 宽度通过 CSS 变量传入样式层（避免散落常量）"
      pattern: "--review-width|420|480|0.67"
    - from: src/components/launcher/parts/LauncherQueueSummaryPill.vue
      to: src/components/launcher/parts/LauncherSearchPanel.vue
      via: "SearchPanel 挂载 pill 并向上冒泡 toggle-staging"
      pattern: "queue summary|toggle-staging|staged.*count"
---

<objective>
把主窗口从“左搜索 + 右侧常驻 staging”过渡为 **单焦点搜索态**：在搜索区提供 queue summary pill 作为 Review 入口；同时建立 Review 宽度口径（`2/3` + `clamp(420~480)`）并禁止“首次入队自动打开 Review”，为后续 overlay 结构接入打底。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-review-overlay/14-CONTEXT.md
@.planning/phases/14-review-overlay/14-RESEARCH.md

@docs/ui-redesign/10-b4-component-architecture.md
@docs/ui-redesign/11-b4-visual-spec.md

@src/components/launcher/LauncherWindow.vue
@src/components/launcher/types.ts
@src/components/launcher/parts/LauncherSearchPanel.vue
@src/components/launcher/parts/LauncherStagingPanel.vue
@src/composables/execution/useCommandExecution/helpers.ts
@src/composables/launcher/useLauncherLayoutMetrics.ts
@src/composables/launcher/useLauncherSessionState.ts
@src/composables/launcher/useWindowSizing/calculation.ts
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 queue summary pill（搜索区内）并作为进入 Review 的入口</name>
  <files>src/components/launcher/parts/LauncherQueueSummaryPill.vue, src/components/launcher/parts/LauncherSearchPanel.vue, src/components/launcher/types.ts, src/components/launcher/LauncherWindow.vue, src/styles.css</files>
  <action>
按 B4 组件建议新增 `LauncherQueueSummaryPill.vue`（参考 `docs/ui-redesign/10-b4-component-architecture.md` 与 `11-b4-visual-spec.md`）：
1) pill 只在 `stagedCommands.length > 0` 时渲染；为空时不渲染且不占位；
2) pill 文案建议使用现有 i18n：`t('launcher.queueTitle', { count })`；aria 使用 `t('launcher.queueToggleAria', { count })`；
3) pill 点击：向上 emit 一个事件用于打开 Review（本阶段先沿用既有的 `toggle-staging` 事件链路，避免大范围事件重命名）；
4) 将 pill 挂载到 `LauncherSearchPanel.vue`（搜索区内，靠近搜索输入但不抢主视觉）；必要时在 `types.ts` 为 SearchPanel 增加 props：`stagedCommandCount`（或等价命名）与 emit；
5) 样式上遵循规格：高度约 28~32px、圆角 999px、字体 12px、padding 10~12px；并确保 `-webkit-app-region: no-drag`。
  </action>
  <verify>
手动用例（与验收矩阵一致）：
1) 打开主窗口（空队列）→ pill 不出现；
2) 任意方式入队 1 条命令 → pill 出现且显示数量（例如“队列 1”）；
3) 点击 pill → Review 打开（本阶段若仍是旧 staging 面板也允许；Phase 14-02 会改为 overlay）。
  </verify>
  <done>
Search 区具备“队列存在”的轻量入口，且满足“仅队列非空显示、不占位、可达性 label 可信”的约束。
  </done>
</task>

<task type="auto">
  <name>Task 2: 禁止“首次入队自动打开 Review”，并确保打开主窗口默认仍为单焦点搜索态</name>
  <files>src/composables/execution/useCommandExecution/helpers.ts, src/composables/launcher/useLauncherSessionState.ts</files>
  <action>
1) 修改 `appendToStaging()`：移除/替换 “`wasEmpty` → `openStagingDrawer()`” 的自动打开逻辑；改为仅更新队列与 staged feedback，让用户仍停留在搜索态（符合 `SHELL-01` + `REV-01` 的单焦点原则）。\n
2) 处理“打开主窗口默认态”与 session restore 的冲突：`useLauncherSessionState` 目前会在 `restored.stagingExpanded` 时调用 `openStagingDrawer()`。\n
   - Phase 14 的目标是“打开 launcher 主窗口只看到搜索态”，因此本阶段应当 **只恢复队列数据**，不主动恢复 Review 打开态。\n
   - 保留 `stagingExpanded` 的持久化字段用于未来兼容（不改 schema），但 restore 时不再自动打开。\n
3) 不引入静默回退/吞错：若有行为变化导致测试断言需要调整，留给 Plan 14-03 统一更新回归。
  </action>
  <verify>
手动用例：
1) 从空队列入队 1 条 → 不应自动打开 Review；pill 出现即可；\n
2) 刷新页面/重启应用后（session restore 生效）：若队列非空，应恢复队列数量，但主窗口仍保持搜索态（Review 默认关闭）。
  </verify>
  <done>
入队不再打断搜索；session restore 不再让 Review 默认打开，满足单焦点搜索态基线。
  </done>
</task>

<task type="auto">
  <name>Task 3: 建立 Review 面板宽度口径（2/3 + clamp 420~480）并集中管理常量</name>
  <files>src/composables/launcher/useLauncherLayoutMetrics.ts, src/composables/launcher/useWindowSizing/calculation.ts, src/styles.css</files>
  <action>
1) 在 `useLauncherLayoutMetrics.ts` 中引入 Review 宽度计算（命名可为 `reviewWidth` / `reviewOverlayWidth`）：\n
   - `ratio = 2/3`（建议写为 `0.67` 但必须以命名常量集中管理）\n
   - `min = 420`, `max = 480`\n
   - `reviewWidth = clamp(searchMainWidth * ratio, min, max)`（按 CONTEXT 锁定决策）\n
2) 将该宽度通过 `searchShellStyle` 注入为 CSS 变量（例如 `--review-width`），供 CSS 与组件统一使用；禁止在多个组件/CSS 里重复写 `0.67/420/480`。\n
3) 为满足 `VIS-03`，在 Review 打开时实际渲染宽度必须落入 `420~480` 范围：\n
   - 若仍沿用 staging 宽度变量/布局，请将其输入改为 `reviewWidth`（先兼容旧命名，命名收口留到后续 Phase）。\n
4) `SHELL-01` 的“单列搜索基线”：当 Review 关闭时不再保留右侧 collapsed 宽度占位（72px）——可以将 collapsed width 归零，或改为仅在 Review 打开时挂载右侧 UI（以避免常驻并列工作区观感）。
  </action>
  <verify>
手动：打开 Review 后，通过 devtools 或临时日志确认 `--review-width` 落在 420~480 范围；且在 Review 关闭时窗口/壳层不再保留右侧“窄列”。
  </verify>
  <done>
Review 宽度口径形成单一真相（single source of truth），并且 UI 层实际落到可读区间。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 搜索态默认无常驻右栏（单焦点）
- [ ] queue summary pill 仅队列非空时出现且可打开 Review
- [ ] 入队不再自动打开 Review
- [ ] Review 宽度口径为 `2/3 + clamp(420~480)` 且常量集中管理
</verification>

<success_criteria>
本计划完成后，Phase 14 的“入口与宽度底座”已具备：用户默认进入搜索态，队列通过 pill 提示并可进入 Review；Review 面板宽度不再受旧 300px 右栏限制，为后续 overlay + 背景锁定落地提供基础。
</success_criteria>

<output>
After completion, create `.planning/phases/14-review-overlay/14-01-SUMMARY.md`.
</output>

