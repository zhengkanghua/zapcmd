---
phase: 14-review-overlay
plan: "03"
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - src/__tests__/app.hotkeys.test.ts
  - src/__tests__/app.core-path-regression.test.ts
  - src/__tests__/app.failure-events.test.ts
  - src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts
  - src/composables/__tests__/launcher/useLauncherSessionState.test.ts
  - src/components/launcher/parts/__tests__/LauncherSearchPanel.floor-height.test.ts
  - src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SIZE-02, REV-01, REV-02, REV-03, VIS-03]
must_haves:
  truths:
    - "自动化回归不再依赖旧 staging 侧栏选择器（`.staging-chip__count` / `.staging-panel--*`），而是验证 B4 的 pill + Review overlay 结构。"
    - "核心路径回归覆盖：入队不自动打开 Review、pill 可打开 Review、Review 内可见摘要、执行/清空/删除/排序不回归。"
    - "背景锁定具备可定位断言：Review 打开时 search input 不可聚焦/不可点击（至少在测试层能证明焦点不会被送回背景）。"
    - "`npm run check:all` 全绿，确保 Phase 14 合入不破坏门禁。"
  artifacts:
    - path: src/__tests__/app.hotkeys.test.ts
      provides: "热键与关闭链路回归（Review 取代旧 staging）"
      contains: "toggleQueue 打开/关闭 Review；Escape 分层后退不隐藏主窗（若已具备）"
    - path: src/__tests__/app.core-path-regression.test.ts
      provides: "搜索 → 入队 → 打开 Review → 执行队列 → 清空 的端到端回归"
      contains: "pill 入口、摘要呈现、队列执行请求断言"
    - path: src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts
      provides: "Review overlay 组件级语义回归"
      contains: "scrim hit-zone、内部滚动容器存在、摘要/复制按钮存在、空态不自动关闭"
---

<objective>
同步并补齐 Phase 14 的自动化回归：将旧 staging 侧栏相关的测试迁移到 B4 Review overlay 结构；新增 Review 组件级测试与关键分支断言，并确保 `npm run check:all` 全绿，为后续 Phase 15 键盘/焦点收口提供稳定基线。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-review-overlay/14-CONTEXT.md
@.planning/phases/14-review-overlay/14-RESEARCH.md

@docs/ui-redesign/12-b4-acceptance-matrix.md

@src/__tests__/app.hotkeys.test.ts
@src/__tests__/app.core-path-regression.test.ts
@src/__tests__/app.failure-events.test.ts
@src/components/launcher/LauncherWindow.vue
@src/components/launcher/parts/LauncherSearchPanel.vue
@src/components/launcher/parts/LauncherQueueSummaryPill.vue
@src/components/launcher/parts/LauncherReviewOverlay.vue
@src/composables/launcher/useLauncherHitZones.ts
@src/composables/execution/useCommandExecution/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 App UI 回归测试：从 staging panel/chip 改为 pill + Review overlay</name>
  <files>src/__tests__/app.hotkeys.test.ts, src/__tests__/app.core-path-regression.test.ts, src/__tests__/app.failure-events.test.ts</files>
  <action>
更新现有回归测试的断言与交互路径：\n
1) 将 `.staging-chip__count` 相关断言替换为 queue summary pill 的新选择器（例如 `.queue-summary-pill__count`，以最终实现为准）。\n
2) 因 Phase 14 禁止“入队自动打开 Review”，测试中需要显式打开 Review：\n
   - 通过点击 pill；或\n
   - 通过既有热键（例如 `toggleQueue`/`Tab`）打开。\n
3) 结构变化后，`staging-panel--opening/open/closing/closed` 断言应迁移到 Review overlay 的状态类名/可见性（例如 `.review-overlay--open`，以最终实现为准）。\n
4) 核心路径（core-path-regression）：入队 → 打开 Review → 断言摘要呈现（不再直接断言 `.staging-card code` 铺完整命令）→ Ctrl+Enter 执行队列 → 队列清空（pill count 归零）。\n
5) failure-events：执行失败时队列不丢失（count 不变），并且 Review 相关结构仍可打开查看。
  </action>
  <verify>
运行：\n
`npm run test:run -- src/__tests__/app.hotkeys.test.ts`\n
`npm run test:run -- src/__tests__/app.core-path-regression.test.ts`\n
`npm run test:run -- src/__tests__/app.failure-events.test.ts`
  </verify>
  <done>
App 级回归测试与 B4 结构对齐，并覆盖“入队不自动打开 + pill 入口 + Review 审阅 + 队列执行/失败不丢失”等关键路径。
  </done>
</task>

<task type="auto">
  <name>Task 2: 新增 Review overlay 组件级测试，锁定 hit-zones/空态/摘要策略等关键语义</name>
  <files>src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts</files>
  <action>
新增组件测试（使用 `@vue/test-utils` mount），覆盖：\n
1) scrim 与 panel 根节点带 `data-hit-zone=\"overlay\"`（防止 blank click hide）；\n
2) Review 打开时存在内部滚动容器（class/role 以实现为准），并且 header/footer 固定结构存在；\n
3) 长命令默认展示摘要（例如包含 `...` 或被截断的 token），并通过 `title` 或等价属性能获取完整命令；\n
4) 每条卡片存在复制按钮（不需要真的写剪贴板，但需要事件/handler 存在）；\n
5) 队列清空时：Review 仍渲染空态且不自动关闭（可通过 props 传入空数组验证）。
  </action>
  <verify>
运行：\n
`npm run test:run -- src/components/launcher/parts/__tests__/LauncherReviewOverlay.test.ts`
  </verify>
  <done>
Review overlay 的关键语义被组件测试锁定，未来 Phase 15/16 改造不会轻易回归。
  </done>
</task>

<task type="auto">
  <name>Task 3: 收口门禁：确保 check:all 全绿，并补齐 layout/session 相关单测的必要调整</name>
  <files>src/composables/__tests__/launcher/useLauncherLayoutMetrics.test.ts, src/composables/__tests__/launcher/useLauncherSessionState.test.ts, src/components/launcher/parts/__tests__/LauncherSearchPanel.floor-height.test.ts</files>
  <action>
1) 若 Plan 14-01/02 改动影响到 `useLauncherLayoutMetrics` / `useLauncherSessionState` 的既有测试断言，按新的 B4 口径更新测试（尤其是：collapsed width 不再占位、session restore 不再自动打开 Review）。\n
2) 确认 Phase 13 的 “无假结果 DOM + filler aria-hidden” 约束仍成立（`LauncherSearchPanel.floor-height.test.ts` 不应因 B4 改造被破坏）。\n
3) 运行全门禁：`npm run check:all`，必须全绿。
  </action>
  <verify>
`npm run check:all`
  </verify>
  <done>
Phase 14 相关改动全部通过项目门禁，且不会破坏 Phase 13 floor-height 的关键契约。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] app.hotkeys/core-path/failure-events 回归通过
- [ ] Review overlay 组件测试通过
- [ ] layoutMetrics/sessionState/floor-height 相关单测通过（必要时更新断言）
- [ ] `npm run check:all` 全绿
</verification>

<success_criteria>
本计划完成后，Phase 14 的结构改动拥有稳定的自动化回归护栏；后续 Phase 15 的键盘/焦点收口可以在可定位的失败输出下推进，而不是依赖肉眼手测。
</success_criteria>

<output>
After completion, create `.planning/phases/14-review-overlay/14-03-SUMMARY.md`.
</output>

