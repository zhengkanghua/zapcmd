---
phase: 10-macos-e2e
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e/desktop-smoke.cjs
autonomous: true
requirements: [E2E-02]
must_haves:
    truths:
      - "`desktop-smoke` 在 Windows 与 macOS 都走同一脚本入口，按平台选择 native driver，不再仅限 Windows。"
      - "macOS 侧默认探测 `safaridriver`，缺失时快速失败并输出可执行修复指引。"
      - "非 Windows/macOS 仍保持明确失败（非静默通过），避免把 skip 当 pass。"
      - "最小断言口径保持不变：启动应用 -> 搜索 -> 抽屉出现 -> Esc 关闭并清空。"
      - "本计划仅交付 E2E-02 的 macOS gate 子集（Linux desktop E2E 仍留在后续阶段）。"
    artifacts:
      - path: "scripts/e2e/desktop-smoke.cjs"
        provides: "跨平台（Windows + macOS）桌面 smoke 执行器"
        contains: "platform profile、driver 探测、app 路径分辨、统一日志/截图落盘"
    key_links:
      - from: "scripts/e2e/desktop-smoke.cjs"
        to: "tauri-driver"
        via: "--native-driver 参数注入平台 driver"
        pattern: "spawn(\"tauri-driver\", args)"
---

<objective>
落实 Phase 10 的首个硬门槛：让 `scripts/e2e/desktop-smoke.cjs` 具备 macOS 可执行路径，并保持 Windows 现有行为不回归。

Purpose: 先打通脚本能力，再让本地 gate 与 CI 可复用同一入口。  
Output: 可在 Windows/macOS 运行的统一 desktop smoke 脚本。
Scope note: 对应 `E2E-02` 的“macOS gate 子集”，不在本计划内补齐 Linux desktop E2E。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-macos-e2e/10-CONTEXT.md
@.planning/phases/10-macos-e2e/10-RESEARCH.md

@scripts/e2e/desktop-smoke.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 引入平台画像与 native driver 预检（win32 + darwin）</name>
  <files>scripts/e2e/desktop-smoke.cjs</files>
  <action>
将当前“仅 Windows”路径重构为平台画像分支：
1) 保留 win32 现有 `msedgedriver` 逻辑；  
2) 新增 darwin 分支，优先探测 `safaridriver`（PATH + 常见系统路径）；  
3) 非 win32/darwin 继续明确失败，并保留错误提示“该平台暂不支持”。

要求：
- 预检顺序固定：`tauri-driver` -> native driver -> app path；
- 任一预检失败都返回非 0，并给出可执行修复步骤。
  </action>
  <verify>
1) 在 Windows 本机执行 `npm run e2e:desktop:smoke`（回归检查，不得弱化现有能力）；  
2) 在 macOS（本机或 CI）执行 `npm run e2e:desktop:smoke`，观察 driver 预检是否走 `safaridriver` 路径；  
3) 在非支持平台执行脚本时，必须明确失败而非静默跳过。  
  </verify>
  <done>
脚本具备 win32/darwin 分支能力，且失败日志对 driver 缺失/不可用给出可执行指引。  
  </done>
</task>

<task type="auto">
  <name>Task 2: app 路径与启动参数跨平台化（保持单入口）</name>
  <files>scripts/e2e/desktop-smoke.cjs</files>
  <action>
补齐应用可执行路径分辨：
- 继续支持 `ZAPCMD_E2E_APP_PATH` 覆盖；  
- 默认路径按平台分辨（Windows `zapcmd.exe`，macOS `zapcmd`）；  
- 错误时输出对应平台构建提示（统一使用 `npm run tauri:build:debug`）。

保持以下约束：
- `tauri-driver` 启动/关闭逻辑不拆脚本；
- 成功/失败产物仍写入 `.tmp/e2e/desktop-smoke/`。
  </action>
  <verify>
1) 设置与不设置 `ZAPCMD_E2E_APP_PATH` 两种模式都可得到明确路径日志；  
2) 可执行文件不存在时，错误提示需包含修复动作与当前解析路径。  
  </verify>
  <done>
Windows/macOS 都能解析 app 路径，且错误提示可直接指导修复。  
  </done>
</task>

<task type="auto">
  <name>Task 3: macOS Go/No-Go 可行性闸门与失败诊断闭环</name>
  <files>scripts/e2e/desktop-smoke.cjs</files>
  <action>
在保持现有用例断言不膨胀的前提下，确保跨平台诊断一致，并形成可停机闭环：
- 关键日志继续输出到 `e2e.log`；
- `tauri-driver.log` tail 采集继续保留；
- 失败截图策略保持现有行为。

额外要求：
- 平台、driver、app path 三项在日志中可见，便于 CI 排障；
- 不新增业务流程断言，只保证最小 smoke 口径；
- 明确 Go/No-Go：macOS 必须能“建立 WebDriver session + 跑通最小 smoke”才允许进入后续 plans；若失败，输出 blocker 结论并中止后续执行。
  </action>
  <verify>
1) 在 macOS 环境执行 `npm run e2e:desktop:smoke`：必须成功建立 session 并跑通最小 smoke（Go）；  
2) 若在 macOS 失败，`.tmp/e2e/desktop-smoke/` 中必须可见 `e2e.log`（含平台+driver+app path）与 `tauri-driver.log` tail（No-Go 结论可定位）；  
3) 成功路径仍打印 `desktop-smoke 成功 ✅`。  
  </verify>
  <done>
已形成可停机闸门：macOS PoC 成功才继续 Phase 10 后续计划；失败时可直接定位根因并阻断推进。  
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/e2e/desktop-smoke.cjs` 在 win32/darwin 分支都存在且路径清晰
- [ ] 非支持平台不会被误判为通过
- [ ] 产物目录仍为 `.tmp/e2e/desktop-smoke/`
</verification>

<success_criteria>
`desktop-smoke.cjs` 从 Windows-only 演进为 Windows + macOS 可执行入口，并以 macOS Go/No-Go 结果作为后续计划推进前置条件（E2E-02 的 macOS 子集）。
</success_criteria>

<output>
After completion, create `.planning/phases/10-macos-e2e/10-01-SUMMARY.md`
</output>
