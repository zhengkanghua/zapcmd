---
phase: 10-macos-e2e
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/verify-local-gate.mjs
  - scripts/README.md
autonomous: true
requirements: [E2E-02]
must_haves:
    truths:
      - "`npm run verify:local` 在 macOS 默认尝试执行桌面 smoke，不再静默跳过。"
      - "Windows 自动安装 `tauri-driver/msedgedriver` 的既有行为保持可用。"
      - "macOS 当前置条件不满足时，`verify:local` 会失败并输出明确修复指引。"
      - "`--skip-e2e` / `--e2e-only` 等调试开关语义保持不变。"
    artifacts:
      - path: "scripts/verify-local-gate.mjs"
        provides: "本地一键门禁（Windows + macOS）行为编排"
        contains: "平台分支、前置校验、失败退出策略、命令行开关保持兼容"
      - path: "scripts/README.md"
        provides: "本地验证脚本行为说明"
        contains: "macOS 默认执行 smoke、前置条件、常见失败处理"
    key_links:
      - from: "scripts/verify-local-gate.mjs"
        to: "scripts/e2e/desktop-smoke.cjs"
        via: "npm run e2e:desktop:smoke"
        pattern: "runDesktopSmokeIfNeeded"
---

<objective>
把 macOS 也纳入本地一键门禁，让贡献者在提交前可直接验证桌面 smoke，同时保持 Windows 既有行为稳定。

Purpose: 对齐“本地先过再提 PR”的跨平台体验。  
Output: `verify:local` 在 Windows/macOS 的一致阻断语义 + 脚本文档更新。
Scope note: 对应 `E2E-02` 的 macOS gate 子集（Linux desktop E2E 不在本计划内）。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-macos-e2e/10-CONTEXT.md
@.planning/phases/10-macos-e2e/10-RESEARCH.md
@.planning/phases/10-macos-e2e/10-01-SUMMARY.md

@scripts/verify-local-gate.mjs
@scripts/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 调整 verify:local 的平台策略（macOS 默认跑 smoke）</name>
  <files>scripts/verify-local-gate.mjs</files>
  <action>
在不破坏现有参数兼容性的前提下，更新平台行为：
1) Windows 与 macOS 默认都执行 `runDesktopSmokeIfNeeded()`；  
2) 非 Windows/macOS 仍默认跳过（若启用严格开关则失败）；  
3) macOS 路径下不引入额外安装脚本，采用“预检 + 明确报错 + 失败退出”策略。

要求：
- 保持 `--skip-e2e`、`--e2e-only`、`--require-windows-e2e`（如命名需演进，需兼容旧参数）；
- 日志明确标识当前平台与执行/跳过原因。
  </action>
  <verify>
1) `npm run verify:local -- --dry-run` 在 Windows/macOS 下都能显示将执行桌面 smoke；  
2) `npm run verify:local -- --skip-e2e --dry-run` 继续只跑质量门禁；  
3) 非支持平台在严格模式下保持失败语义。  
  </verify>
  <done>
`verify:local` 在 macOS 默认纳入桌面 smoke，且参数语义兼容历史使用方式。  
  </done>
</task>

<task type="auto">
  <name>Task 2: 保持 Windows 自动安装链路稳定并最小化平台分歧</name>
  <files>scripts/verify-local-gate.mjs</files>
  <action>
确认并收敛安装逻辑：
- Windows 仍自动安装 `tauri-driver` + `msedgedriver`（缺失或显式安装参数时触发）；  
- macOS 不走 Windows 安装分支，避免误调用 PowerShell installer；  
- 错误提示按平台输出可执行修复动作。

确保：
- 不改变既有 Windows 成功路径；
- 不新增与 Phase 边界无关的 Linux desktop E2E 逻辑。
  </action>
  <verify>
1) Windows `--dry-run` 可见安装分支命令仍是 `cargo install tauri-driver --locked` + `install-msedgedriver.ps1`；  
2) macOS `--dry-run` 不会出现 `install-msedgedriver.ps1` 安装步骤。  
  </verify>
  <done>
Windows 自动安装链路无回归，macOS 分支逻辑清晰且不会误走 Windows 流程。  
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新脚本文档（本地可执行前置与排障）</name>
  <files>scripts/README.md</files>
  <action>
更新 `scripts/README.md` 中本地 gate 章节：
- 说明 `npm run verify:local` 在 macOS 默认执行桌面 smoke；  
- 补充 macOS 前置条件（`tauri-driver`、`safaridriver` 可用性）与常见失败定位方式；  
- 保留 Windows 自动安装说明与参数示例。

要求：
- 文档示例与脚本真实参数保持一致；
- 不新增与当前 phase 无关的发布/版本管理内容。
  </action>
  <verify>
1) `scripts/README.md` 命令示例可直接执行（参数名与脚本一致）；  
2) 文档覆盖 Windows + macOS 本地验证路径。  
  </verify>
  <done>
脚本文档可让贡献者在 macOS 本地完成 smoke 预检与排障，不再依赖隐式知识。  
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `verify:local` 的默认行为满足“Windows + macOS 都跑桌面 smoke”
- [ ] Windows 自动安装逻辑未回归
- [ ] `scripts/README.md` 与脚本参数一致
</verification>

<success_criteria>
贡献者在 macOS 上执行 `npm run verify:local` 即可触发桌面 smoke，并在失败时获得可执行修复指引（E2E-02 的 macOS 子集）。
</success_criteria>

<output>
After completion, create `.planning/phases/10-macos-e2e/10-02-SUMMARY.md`
</output>
