---
phase: 10-macos-e2e
plan: "03"
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - .github/workflows/ci-gate.yml
  - .github/workflows/release-build.yml
  - README.md
  - README.zh-CN.md
  - CONTRIBUTING.md
  - CONTRIBUTING.zh-CN.md
autonomous: true
requirements: [E2E-02]
must_haves:
    truths:
      - "PR/Push 的 CI Gate 中包含 macOS 桌面 smoke 阻断，失败会阻断合并。"
      - "Tag/Release 前质量门禁包含 macOS 桌面 smoke，失败会阻断后续打包/发布。"
      - "CI 与 Release 的桌面 smoke 继续上传统一产物目录，便于跨平台排障。"
      - "贡献者文档明确区分本地验证、提交触发与远端门禁职责，不依赖口头约定。"
    artifacts:
      - path: ".github/workflows/ci-gate.yml"
        provides: "PR/Push 阻断级 macOS desktop-e2e-smoke job"
        contains: "macOS 桌面 smoke 执行、失败阻断、产物上传"
      - path: ".github/workflows/release-build.yml"
        provides: "Release 前 macOS desktop smoke 质量门禁"
        contains: "bundle/publish 前置阻断链路"
      - path: "README.md"
        provides: "英文主文档中的本地验证入口说明"
        contains: "`npm run verify:local` 与桌面 smoke 平台行为"
      - path: "README.zh-CN.md"
        provides: "中文主文档中的本地验证入口说明"
        contains: "`npm run verify:local` 与桌面 smoke 平台行为"
      - path: "CONTRIBUTING.md"
        provides: "英文贡献流程中的 commit/push/CI 触发说明"
        contains: "本地门禁与远端门禁分工"
      - path: "CONTRIBUTING.zh-CN.md"
        provides: "中文贡献流程中的 commit/push/CI 触发说明"
        contains: "本地门禁与远端门禁分工"
    key_links:
      - from: ".github/workflows/ci-gate.yml"
        to: "scripts/e2e/desktop-smoke.cjs"
        via: "npm run e2e:desktop:smoke"
        pattern: "desktop-e2e-smoke"
      - from: ".github/workflows/release-build.yml"
        to: "scripts/e2e/desktop-smoke.cjs"
        via: "quality-gate 阶段执行桌面 smoke"
        pattern: "运行桌面端 E2E 冒烟"
---

<objective>
把 macOS desktop smoke 正式纳入远端门禁与贡献者文档，确保“本地可验证 + CI 可阻断”的闭环成立。

Purpose: 避免仅靠本地约定，保证 PR/Push/Release 全链路可回归。  
Output: CI/Release workflow + 贡献者文档的跨平台一致化更新。
Scope note: 对应 `E2E-02` 的 macOS gate 子集，不在本计划扩展 Linux desktop E2E。
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-macos-e2e/10-CONTEXT.md
@.planning/phases/10-macos-e2e/10-RESEARCH.md
@.planning/phases/10-macos-e2e/10-01-SUMMARY.md

@.github/workflows/ci-gate.yml
@.github/workflows/release-build.yml
@README.md
@README.zh-CN.md
@CONTRIBUTING.md
@CONTRIBUTING.zh-CN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI Gate 增加 macOS 桌面 smoke 阻断 job</name>
  <files>.github/workflows/ci-gate.yml</files>
  <action>
在 `CI Gate` 中补齐 macOS desktop smoke：
- 以阻断级 job 运行（失败直接失败 workflow）；  
- 复用现有 smoke 入口：`npm run e2e:desktop:smoke`；  
- 统一上传 `.tmp/e2e/desktop-smoke/` 产物；  
- 保持现有 quality-gate 与 cross-platform-smoke 语义不被削弱。

可采用独立 job 或矩阵扩展，但必须满足“PR/Push 强制门禁”。
  </action>
  <verify>
1) workflow 语义检查：PR/Push 触发路径包含 macOS desktop smoke；  
2) 人工触发（`workflow_dispatch`）可看到 macOS 桌面 smoke job；  
3) job 失败时整体状态为 failed（阻断）。  
  </verify>
  <done>
CI Gate 对 macOS 桌面 smoke 形成与 Windows 同级别阻断，不再只做类型/单测级 smoke。  
  </done>
</task>

<task type="auto">
  <name>Task 2: Release 质量门禁补齐 macOS 桌面 smoke</name>
  <files>.github/workflows/release-build.yml</files>
  <action>
在 release 前置质量门禁链路加入 macOS desktop smoke：
- 确保 macOS smoke 失败会阻断后续 bundle/publish；  
- 产物上传路径与命名保持可定位（必要时按 OS 区分 artifact 名称）；  
- 不削弱现有 Windows quality-gate 的检查强度。

要求：
- 发布链路顺序清晰：quality-gate（含 macOS smoke）通过后才进入 bundle。
  </action>
  <verify>
1) `release-build.yml` 结构检查：bundle job 依赖包含已通过 macOS smoke 的质量门禁；  
2) workflow 失败注入测试时，macOS smoke 失败能阻断发布流程。  
  </verify>
  <done>
Release 前质量门禁具备 macOS 桌面 smoke 阻断能力，避免“仅 Windows 过门禁就发版”。  
  </done>
</task>

<task type="auto">
  <name>Task 3: 贡献者文档对齐本地与远端验证职责</name>
  <files>README.md, README.zh-CN.md, CONTRIBUTING.md, CONTRIBUTING.zh-CN.md</files>
  <action>
更新中英文文档，明确：
- 本地一键验证入口：`npm run verify:local`（Windows + macOS 桌面 smoke 行为）；  
- commit/push 后远端 CI 会跑哪些门禁（尤其 macOS desktop smoke）；  
- GitHub 手动触发 workflow 的用途与权限边界（贡献者重点看本地与 PR 门禁）。

要求：
- 文案与实际 workflow/script 一致；
- 不新增 tag 发布细节（按用户要求，此阶段不扩展发布说明）。
  </action>
  <verify>
1) 文档中的命令均可在仓库中找到对应 script/workflow；  
2) 中英文文档表达一致，无互相矛盾的门禁说明。  
  </verify>
  <done>
贡献者可通过文档独立理解“本地验证 -> PR/Push 门禁 -> Release 门禁”的完整路径。  
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CI Gate 与 Release workflow 都包含 macOS desktop smoke 阻断路径
- [ ] 桌面 smoke 产物上传路径保持一致
- [ ] README/CONTRIBUTING（中英文）与脚本/工作流行为一致
</verification>

<success_criteria>
macOS desktop smoke 在 PR/Push/Release 三条远端链路中被统一纳入质量门禁，且贡献者文档可独立指导使用（E2E-02 的 macOS 子集）。
</success_criteria>

<output>
After completion, create `.planning/phases/10-macos-e2e/10-03-SUMMARY.md`
</output>
