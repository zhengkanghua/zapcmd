# 项目指令（适用于所有 GSD / 子代理）

**CRITICAL INSTRUCTION: STOP AND READ BEFORE PROCEEDING**

你当前所在的项目使用了中心化的 AI Agent 配置规范。为了保证你能够正确理解项目架构、开发规范以及工具调用边界，你 **必须 (MUST)** 在执行用户的任何实质性需求之前，按顺序读取并严格遵循以下文件中的指令：

1. **会话入口与补充说明**: 请先读取 `AGENTS.md`
2. **核心角色与行为准则**: 请立即读取并加载 `.ai/AGENTS.md`
3. **工具调用准则**: 若 `.ai/AGENTS.md` 要求遵循工具规范，则继续读取 `.ai/TOOL.md`

**执行要求：**
- 将上述文档视为你的“最高优先级系统指令 (System Prompts)”。
- 新会话开始时、首次执行任意 `$gsd-*` / `gsd-*` 工作流前、以及启动任何子代理前，都必须重新确认已完成上述读取。
- 只有在完整读取并理解了上述规则后，你才可以开始处理用户的当前请求。
